---
phase: 14-seeding-revamp
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - backend/src/main/java/com/shiftleft/hub/common/config/ItSeeder.java
  - backend/src/main/java/com/shiftleft/hub/common/config/PublicSeeder.java
  - backend/src/main/resources/data/seed/kb/it/it-vpn-setup.md
  - backend/src/main/resources/data/seed/kb/it/it-password-policy.md
  - backend/src/main/resources/data/seed/kb/it/it-account-creation.md
  - backend/src/main/resources/data/seed/kb/it/it-email-setup.md
  - backend/src/main/resources/data/seed/kb/it/it-wifi-access.md
  - backend/src/main/resources/data/seed/kb/it/it-printing-guide.md
  - backend/src/main/resources/data/seed/kb/it/it-remote-access.md
  - backend/src/main/resources/data/seed/kb/it/it-software-installation.md
  - backend/src/main/resources/data/seed/kb/it/it-hardware-requests.md
  - backend/src/main/resources/data/seed/kb/it/it-security-incident-reporting.md
  - backend/src/main/resources/data/seed/kb/public/public-company-overview.md
  - backend/src/main/resources/data/seed/kb/public/public-getting-started.md
  - backend/src/main/resources/data/seed/kb/public/public-general-faq.md
  - backend/src/main/resources/data/seed/kb/public/public-announcements.md
  - backend/src/main/resources/data/seed/kb/public/public-support-contacts.md
  - backend/src/main/resources/data/seed/kb/public/public-workplace-policies.md
  - backend/src/main/resources/data/seed/kb/public/public-it-helpdesk-basics.md
  - backend/src/main/resources/data/seed/kb/public/public-hr-overview.md
  - backend/src/main/resources/data/seed/kb/public/public-office-locations.md
  - backend/src/main/resources/data/seed/kb/public/public-company-directory.md
autonomous: true
requirements:
  - SEED-01
  - SEED-03
  - SEED-04
  - SEED-06

must_haves:
  truths:
    - "IT workspace has 12 bilingual tags with hex colors matching the existing tag palette"
    - "Public workspace has 6 bilingual tags with hex colors"
    - "IT workspace has 10 published bilingual articles covering VPN, password, accounts, email, wifi, printing, remote access, software, hardware, and security"
    - "Public workspace has 10 published bilingual articles covering company overview, getting started, FAQ, announcements, support, policies, IT basics, HR overview, locations, and directory"
    - "20 markdown files exist in data/seed/kb/it/ and data/seed/kb/public/"
    - "Each markdown file has valid frontmatter (slug, title_en, title_fr, tags, excerpt, excerpt_fr)"
    - "Each markdown file has bilingual body separated by <!-- FR -->"
    - "All 20 articles are authored by the admin user"
    - "Re-running seeding produces zero duplicates"
  artifacts:
    - path: "backend/src/main/java/com/shiftleft/hub/common/config/ItSeeder.java"
      provides: "IT workspace tag + article seeding"
      contains: "@Order(2), @Profile(\"!test\")"
    - path: "backend/src/main/java/com/shiftleft/hub/common/config/PublicSeeder.java"
      provides: "Public workspace tag + article seeding"
      contains: "@Order(2), @Profile(\"!test\")"
    - path: "backend/src/main/resources/data/seed/kb/it/it-vpn-setup.md"
      provides: "IT seed article — VPN setup"
      contains: "slug: it-vpn-setup"
    - path: "backend/src/main/resources/data/seed/kb/public/public-company-overview.md"
      provides: "Public seed article — company overview"
      contains: "slug: public-company-overview"
  key_links:
    - from: "ItSeeder.java"
      to: "data/seed/kb/it/*.md"
      via: "PathMatchingResourcePatternResolver scanning classpath:data/seed/kb/it/*.md"
    - from: "PublicSeeder.java"
      to: "data/seed/kb/public/*.md"
      via: "PathMatchingResourcePatternResolver scanning classpath:data/seed/kb/public/*.md"
    - from: "ItSeeder.java"
      to: "WorkspaceService.findBySlug()"
      via: "Looks up IT workspace by slug 'it' to get workspace UUID"
    - from: "PublicSeeder.java"
      to: "WorkspaceService.findBySlug()"
      via: "Looks up Public workspace by slug 'public' to get workspace UUID"

user_setup: []
---

<objective>
Create IT and Public workspace seed content — 20 bilingual markdown files organized by workspace subdirectory, plus ItSeeder and PublicSeeder Java classes that read the files and create tags + articles in their respective workspaces.

**Purpose:** Complete the seed data for all 4 workspaces. IT workspace carries forward existing IT-themed content (rewritten). Public workspace provides general company-wide knowledge accessible to all users.
**Output:** 10 `data/seed/kb/it/*.md` files, 10 `data/seed/kb/public/*.md` files, `ItSeeder.java`, `PublicSeeder.java`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-seeding-revamp/14-CONTEXT.md
@.planning/phases/14-seeding-revamp/14-01-PLAN.md
@.planning/phases/14-seeding-revamp/14-02-PLAN.md

# Existing IT tag patterns (for reference — IT tags preserve colors from old KbSeeder)
@backend/src/main/java/com/shiftleft/hub/common/config/KbSeeder.java (being deleted — reference TAG_COLORS, TAG_NAMES_EN, TAG_NAMES_FR patterns)

# Entity references (same as Plan 2)
@backend/src/main/java/com/shiftleft/hub/article/domain/Article.java
@backend/src/main/java/com/shiftleft/hub/tag/domain/Tag.java
@backend/src/main/java/com/shiftleft/hub/workspace/service/WorkspaceService.java

<interfaces>
<!-- Same interface contracts as Plan 2 — reused from codebase -->

From WorkspaceService:
```java
public Optional<Workspace> findBySlug(String slug)
```

From ArticleRepository:
```java
Optional<Article> findBySlug(String slug);
Article save(Article entity);
```

From TagRepository:
```java
List<Tag> findByNameEnIn(Collection<String> nameEn);
Tag save(Tag entity);
```

From UserRepository:
```java
List<User> findByRole(UserRole role);
```

From Article builder:
```java
Article.builder()
    .titleEn(String).titleFr(String)
    .contentEn(String).contentFr(String)
    .slug(String).excerpt(String)
    .status(ArticleStatus.PUBLISHED)
    .viewCount(0).publishedAt(LocalDateTime.now())
    .author(User).tags(Set<Tag>)
    .workspaceId(UUID)
    .build()
```

From Tag builder:
```java
Tag.builder()
    .nameEn(String).nameFr(String).color(String)
    .workspaceId(UUID)
    .build()
```

Redirect for old KbSeeder patterns (TAG_COLORS, TAG_NAMES_EN, TAG_NAMES_FR, SUPPORTED_TAGS):
- These maps are only for reference — the ItSeeder will redefine IT tags with 12 entries (not 14)
- The same file-reading utility pattern (readFile, parseFrontmatter, extractBilingualBody) should be replicated in each seeder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IT workspace seed content (10 markdown files + ItSeeder.java)</name>
  <files>
    backend/src/main/resources/data/seed/kb/it/it-vpn-setup.md
    backend/src/main/resources/data/seed/kb/it/it-password-policy.md
    backend/src/main/resources/data/seed/kb/it/it-account-creation.md
    backend/src/main/resources/data/seed/kb/it/it-email-setup.md
    backend/src/main/resources/data/seed/kb/it/it-wifi-access.md
    backend/src/main/resources/data/seed/kb/it/it-printing-guide.md
    backend/src/main/resources/data/seed/kb/it/it-remote-access.md
    backend/src/main/resources/data/seed/kb/it/it-software-installation.md
    backend/src/main/resources/data/seed/kb/it/it-hardware-requests.md
    backend/src/main/resources/data/seed/kb/it/it-security-incident-reporting.md
    backend/src/main/java/com/shiftleft/hub/common/config/ItSeeder.java
  </files>
  <action>
    **Part A: Create 10 IT markdown files**

    Directory: `backend/src/main/resources/data/seed/kb/it/` (create the directory via `New-Item -ItemType Directory -Force`)

    Same frontmatter + bilingual body format as Plan 2 (identical to existing `connect-to-wifi.md` pattern).

    **IT article topics — rewrite existing IT topics for the new format (agent discretion on exact titles/content):**

    1. **VPN Setup & Configuration** — slug: `it-vpn-setup`. Cover VPN client installation, connection profiles, MFA setup, troubleshooting. Tags: vpn, security.
    2. **Password Policy & Account Security** — slug: `it-password-policy`. Password requirements, MFA enrollment, account lockout, password reset procedures. Tags: password, security.
    3. **New Account Creation** — slug: `it-account-creation`. Account provisioning process, role-based access, system access requests, account lifecycle. Tags: accounts, security.
    4. **Email Setup on Mobile Devices** — slug: `it-email-setup`. Outlook mobile setup, iOS/Android configuration, mailbox access, signature setup. Tags: email, accounts.
    5. **Wi-Fi Network Access** — slug: `it-wifi-access`. Corporate and guest Wi-Fi connection instructions, WPA2-Enterprise setup, troubleshooting. Tags: wifi, networking.
    6. **Printing Guide** — slug: `it-printing-guide`. Printer setup, driver installation, print release, scanning, common troubleshooting. Tags: printing, hardware.
    7. **Remote Access to Intranet** — slug: `it-remote-access`. VPN for remote work, RDC setup, application access, file share connectivity. Tags: remote-access, vpn.
    8. **Software Installation Requests** — slug: `it-software-installation`. Software request process, approved software list, self-service portal, installation policy. Tags: software, incident.
    9. **Hardware Requests & Asset Management** — slug: `it-hardware-requests`. Laptop requests, peripheral ordering, asset tagging, return procedures. Tags: hardware, incident.
    10. **Security Incident Reporting** — slug: `it-security-incident-reporting`. Phishing reporting, suspicious activity, data breach procedures, incident response. Tags: security, incident.

    **IT tag definitions (per D-12, preserving existing 12 tags from KbSeeder, dropping 'mobile' and 'it-requests'):**
    | Key           | Name EN           | Name FR             | Color   |
    |---------------|-------------------|---------------------|---------|
    | vpn           | VPN               | VPN                 | #7c3aed |
    | password      | Password          | Mot de passe        | #dc2626 |
    | security      | Security          | Sécurité            | #dc2626 |
    | networking    | Networking        | Réseau              | #0891b2 |
    | wifi          | Wi-Fi             | Wi-Fi               | #0891b2 |
    | email         | Email             | Courriel            | #2563eb |
    | software      | Software          | Logiciel            | #ca8a04 |
    | printing      | Printing          | Impression          | #16a34a |
    | accounts      | Accounts          | Comptes             | #9333ea |
    | remote-access | Remote Access     | Accès distant       | #7c3aed |
    | incident      | Incident          | Incident            | #dc2626 |
    | hardware      | Hardware          | Matériel            | #16a34a |

    Note: Colors match the existing KbSeeder's `TAG_COLORS` map — preserving visual consistency for IT tags. The `mobile` and `it-requests` tags from the old KbSeeder are NOT included (per D-12 which specifies exactly 12 tags).

    **Part B: Create ItSeeder.java**

    Same structure as HrSeeder/LegalSeeder from Plan 2. Differences:
    - Looks up workspace by slug `"it"` instead of workspace-specific slug
    - Scans markdown from `"classpath:data/seed/kb/it/*.md"`
    - Creates IT tag set (12 tags) instead of HR/Legal sets
    - Same `@Order(2)` execution order

    **Tag idempotency:** Check by `name_en` + workspaceId.
    **Article idempotency:** Check by slug.

    **What NOT to do:**
    - Do NOT include the old KbSeeder's `mobile` or `it-requests` tags — IT workspace gets exactly 12 tags per D-12
    - Do NOT reference or import KbSeeder, HrSeeder, or LegalSeeder — ItSeeder is independent
    - Article slugs MUST be globally unique — all start with `it-` prefix (e.g., `it-vpn-setup`)
    - Same file format and patterns as Plan 2
  </action>
  <verify>
    <automated>$itFiles = Get-ChildItem "backend/src/main/resources/data/seed/kb/it/*.md" 2>$null; if ($itFiles.Count -ne 10) { throw "Expected 10 IT files, found $($itFiles.Count)" } else { echo "OK - 10 IT markdown files exist" }</automated>
    <automated>If (-not (Test-Path "backend/src/main/java/com/shiftleft/hub/common/config/ItSeeder.java")) { throw "ItSeeder.java not found" }</automated>
    <automated>Select-String -Path "backend/src/main/java/com/shiftleft/hub/common/config/ItSeeder.java" -Pattern "@Order\(2\)" -Quiet</automated>
    <automated>$totalTagCount = (Select-String -Path "backend/src/main/java/com/shiftleft/hub/common/config/ItSeeder.java" -Pattern "Tag\.builder\(\)" -AllMatches).Matches.Count; if ($totalTagCount -ne 12) { throw "Expected 12 IT tags, found $totalTagCount" }</automated>
    <automated>Select-String -Path "backend/src/main/resources/data/seed/kb/it/it-vpn-setup.md" -Pattern "slug: it-vpn-setup" -Quiet</automated>
  </verify>
  <done>
    - `data/seed/kb/it/` directory exists with 10 markdown files
    - Each markdown file has valid frontmatter and bilingual body
    - `ItSeeder.java` exists with @Order(2), creates exactly 12 IT tags + 10 articles in IT workspace
    - Tags preserve existing color palette; 'mobile' and 'it-requests' are NOT created
    - Articles authored by admin user, workspaceId set to IT workspace, status PUBLISHED
    - All operations idempotent
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Public workspace seed content (10 markdown files + PublicSeeder.java)</name>
  <files>
    backend/src/main/resources/data/seed/kb/public/public-company-overview.md
    backend/src/main/resources/data/seed/kb/public/public-getting-started.md
    backend/src/main/resources/data/seed/kb/public/public-general-faq.md
    backend/src/main/resources/data/seed/kb/public/public-announcements.md
    backend/src/main/resources/data/seed/kb/public/public-support-contacts.md
    backend/src/main/resources/data/seed/kb/public/public-workplace-policies.md
    backend/src/main/resources/data/seed/kb/public/public-it-helpdesk-basics.md
    backend/src/main/resources/data/seed/kb/public/public-hr-overview.md
    backend/src/main/resources/data/seed/kb/public/public-office-locations.md
    backend/src/main/resources/data/seed/kb/public/public-company-directory.md
    backend/src/main/java/com/shiftleft/hub/common/config/PublicSeeder.java
  </files>
  <action>
    **Part A: Create 10 Public markdown files**

    Directory: `backend/src/main/resources/data/seed/kb/public/` (create via `New-Item -ItemType Directory -Force`)

    Same frontmatter + bilingual body format as all other seeders.

    **Public article topics (company-wide knowledge accessible to all users):**

    1. **Company Overview** — slug: `public-company-overview`. Company mission, history, values, leadership team, organizational structure. Tags: general, getting-started.
    2. **Getting Started Guide** — slug: `public-getting-started`. First-day orientation, systems access overview, key contacts, onboarding portal. Tags: getting-started, general.
    3. **General FAQ** — slug: `public-general-faq`. Common questions about company policies, benefits, IT support, office procedures. Tags: faq, general.
    4. **Company Announcements** — slug: `public-announcements`. How to find announcements, newsletter subscriptions, all-hands meetings, communication channels. Tags: announcements, general.
    5. **Support Contacts** — slug: `public-support-contacts`. IT helpdesk, HR support, facilities, payroll inquiries, emergency contacts. Tags: support, general.
    6. **Workplace Policies Overview** — slug: `public-workplace-policies`. Summary of key policies: code of conduct, remote work, dress code, attendance. Tags: policies, getting-started.
    7. **IT Helpdesk Basics** — slug: `public-it-helpdesk-basics`. How to submit a ticket, supported services, response times, self-service portal. Tags: support, faq.
    8. **HR Overview for Employees** — slug: `public-hr-overview`. HR department services, how to request time off, benefits enrollment contacts, payroll questions. Tags: support, general.
    9. **Office Locations & Facilities** — slug: `public-office-locations`. Office addresses, directions, parking, building access, floor plans, amenities. Tags: general, getting-started.
    10. **Company Directory** — slug: `public-company-directory`. How to find colleagues, department directory, org chart access, contact information. Tags: getting-started, announcements.

    **Public tag definitions (per D-13, D-14):**
    | Key             | Name EN           | Name FR        | Color   |
    |-----------------|-------------------|----------------|---------|
    | general         | General           | Général        | #2563eb |
    | announcements   | Announcements     | Annonces       | #ea580c |
    | faq             | FAQ               | FAQ            | #0891b2 |
    | getting-started | Getting Started   | Démarrage      | #16a34a |
    | support         | Support           | Support        | #9333ea |
    | policies        | Policies          | Politiques     | #7c3aed |

    Note: The `policies` and `general` tag names may overlap with HR tag names — this is fine because tags have workspace_id scope.

    **Part B: Create PublicSeeder.java**

    Same structure as all other per-workspace seeders. Differences:
    - Looks up workspace by slug `"public"` instead of workspace-specific slug
    - Scans markdown from `"classpath:data/seed/kb/public/*.md"`
    - Creates Public tag set (6 tags) instead of other workspace sets
    - Same `@Order(2)` execution order

    **What NOT to do:**
    - Do NOT create articles with slugs that overlap with old IT seed slugs — old slugs were `connect-corporate-wifi`, `software-installation-request`, etc., which are all `it-*` prefixed. Public articles use `public-*` prefix — no overlap possible.
    - Do NOT reference or import other seeder classes
    - Article slugs MUST be globally unique — all start with `public-` prefix
  </action>
  <verify>
    <automated>$publicFiles = Get-ChildItem "backend/src/main/resources/data/seed/kb/public/*.md" 2>$null; if ($publicFiles.Count -ne 10) { throw "Expected 10 Public files, found $($publicFiles.Count)" } else { echo "OK - 10 Public markdown files exist" }</automated>
    <automated>If (-not (Test-Path "backend/src/main/java/com/shiftleft/hub/common/config/PublicSeeder.java")) { throw "PublicSeeder.java not found" }</automated>
    <automated>Select-String -Path "backend/src/main/java/com/shiftleft/hub/common/config/PublicSeeder.java" -Pattern "@Order\(2\)" -Quiet</automated>
    <automated>$totalTagCount = (Select-String -Path "backend/src/main/java/com/shiftleft/hub/common/config/PublicSeeder.java" -Pattern "Tag\.builder\(\)" -AllMatches).Matches.Count; if ($totalTagCount -ne 6) { throw "Expected 6 Public tags, found $totalTagCount" }</automated>
    <automated>Select-String -Path "backend/src/main/resources/data/seed/kb/public/public-company-overview.md" -Pattern "slug: public-company-overview" -Quiet</automated>
    <automated>$total = (Get-ChildItem "backend/src/main/resources/data/seed/kb/hr/*.md").Count + (Get-ChildItem "backend/src/main/resources/data/seed/kb/legal/*.md").Count + (Get-ChildItem "backend/src/main/resources/data/seed/kb/it/*.md").Count + (Get-ChildItem "backend/src/main/resources/data/seed/kb/public/*.md").Count; if ($total -ne 40) { throw "Expected 40 total files across all workspaces, found $total" } else { echo "OK - 40 total markdown files across all 4 workspaces" }</automated>
  </verify>
  <done>
    - `data/seed/kb/public/` directory exists with 10 markdown files
    - Each markdown file has valid frontmatter and bilingual body
    - `PublicSeeder.java` exists with @Order(2), creates 6 Public tags + 10 articles in Public workspace
    - Tags have workspaceId set to Public workspace
    - Articles authored by admin user, workspaceId set to Public workspace, status PUBLISHED
    - All operations idempotent
    - Maven compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → Seeder | Markdown files on classpath read by seeder at startup. Version-controlled application resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | IT/Public markdown files | accept | Version-controlled application resources. No external write access. Defensive frontmatter parsing. |
| T-14-08 | Information Disclosure | Seed article content | accept | Fictional demo data for all workspaces. No real company data. |
| T-14-09 | Denial of Service | Seeder startup delay | accept | ~40 articles across 4 seeders is negligible at startup. Total < 200 JPA operations. |
</threat_model>

<verification>
1. ✅ 20 markdown files created (10 IT + 10 Public) in correct subdirectories
2. ✅ ItSeeder.java and PublicSeeder.java created with @Order(2)
3. ✅ IT workspace gets exactly 12 tags (not 14 — 'mobile' and 'it-requests' excluded per D-12)
4. ✅ Public workspace gets exactly 6 tags
5. ✅ All articles use globally unique slugs (it-* and public-* prefixes)
6. 🔲 Total: 40 markdown files across all 4 workspaces
7. 🔲 Maven build compiles without errors (`mvn compile -f backend/pom.xml`)
</verification>

<success_criteria>
- 40 markdown files exist across all 4 workspace subdirectories (10 each)
- ItSeeder.java and PublicSeeder.java compile without errors
- All per-workspace seeders are independent — no cross-seeder imports
- Total seed data: 7 users, 4 workspaces, 34 tags (8 HR + 8 Legal + 12 IT + 6 Public), 40 articles
- Zero compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-seeding-revamp/14-03-SUMMARY.md`
</output>
