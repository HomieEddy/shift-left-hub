---
phase: 21-production-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - "frontend/public/env.js"
  - "docs/demo-walkthrough.md"
autonomous: false
requirements: [DEP-01, DEP-02, DEP-03, DEP-04, DEP-05]
user_setup: []
must_haves:
  truths:
    - "Frontend env.js is updated with the Railway backend URL"
    - "Demo walkthrough references production URLs instead of localhost"
    - "Code changes committed and pushed to main"
    - "Vercel and Railway auto-deploy from the latest main commit"
    - "Full demo walkthrough works against production URLs (login, KB search, AI chat, escalate, agent resolve, KCS draft)"
    - "All DEP-01 through DEP-05 criteria are satisfied"
  artifacts:
    - path: "frontend/public/env.js"
      provides: "Production Railway URL set for runtime API calls"
      contains: "apiBaseUrl: 'https://"
    - path: "docs/demo-walkthrough.md"
      provides: "Updated production URLs for manual smoke test"
      min_lines: 110
  key_links:
    - from: "env.js"
      to: "Railway backend"
      via: "window.__env.apiBaseUrl"
      pattern: "apiBaseUrl"
    - from: "demo-walkthrough.md"
      to: "Production URLs"
      via: "Updated URLs point to Vercel instead of localhost:4200"
      pattern: "vercel.app"
---

<objective>
**Set the production Railway backend URL, update the demo walkthrough for production URLs, trigger deployment, and manually verify the full application works against live URLs.**

Purpose: Complete the deployment by wiring the frontend to the Railway backend, pushing the final config to main (which triggers auto-deploy on Vercel and Railway), and running the demo walkthrough against production URLs to verify DEP-01 through DEP-05.

Output: Live production application on Vercel (frontend) + Railway (backend/DB), verified via full demo walkthrough.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-production-deployment/21-CONTEXT.md
@docs/demo-walkthrough.md

The Railway backend URL and Vercel frontend URL are known after Plan 21-02 completes.
The user provides these URLs when they resume from Plan 21-02.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set production Railway URL in env.js and update demo walkthrough</name>
  <files>
    - "frontend/public/env.js"
    - "docs/demo-walkthrough.md"
  </files>
  <action>
    **Update production configuration and documentation, then push to main.**

    **Step 1 — Update `frontend/public/env.js` with the Railway backend URL**

    Read `frontend/public/env.js`. Currently has:
    ```javascript
    (window.__env || (window.__env = {})).apiBaseUrl = '';
    ```

    Replace `''` with the Railway backend URL from the user:
    ```javascript
    (window.__env || (window.__env = {})).apiBaseUrl = 'https://[your-railway-app].up.railway.app';
    ```

    Remove the trailing slash. Per D-06, this is the default Railway domain. Example format: `https://shiftleft-backend-production.up.railway.app`

    **Step 2 — Update `docs/demo-walkthrough.md` for production URLs**

    Read the current `docs/demo-walkthrough.md`. It currently references `http://localhost:4200` for all steps.

    Create a new section at the top titled "## Production Verification" with the production URLs:

    ```markdown
    ## Production Verification

    After deploying to production, use these URLs instead of localhost:

    - **Frontend:** `https://[your-vercel-app].vercel.app`
    - **Backend Health:** `https://[your-railway-app].up.railway.app/actuator/health`

    All steps below use `https://[your-vercel-app].vercel.app` as the base URL instead of `http://localhost:4200`.
    ```

    Add a note at the top of the "Prerequisites" section that references the production URLs:

    Replace the existing "## Prerequisites" section:
    ```
    ## Prerequisites

    - Docker Compose running: `docker compose up`
    - Java 21 + Maven for backend: `cd backend && ./mvnw spring-boot:run`
    - Node.js 22+ for frontend: `cd frontend && pnpm install && pnpm start`
    - Browser: Chrome (for best Playwright compatibility)
    ```

    With:
    ```
    ## Prerequisites

    ### Local Development
    - Docker Compose running: `docker compose up`
    - Java 21 + Maven for backend: `cd backend && ./mvnw spring-boot:run`
    - Node.js 22+ for frontend: `cd frontend && pnpm install && pnpm start`
    - Browser: Chrome (for best Playwright compatibility)

    ### Production Verification
    - Frontend deployed on Vercel: `https://[your-vercel-app].vercel.app`
    - Backend deployed on Railway: `https://[your-railway-app].up.railway.app`
    - Both services have built and deployed from the latest `main` commit
    ```

    Do NOT change the original localhost walkthrough steps — keep them as-is for local development. The production URL note at the top is sufficient for the user to substitute.

    **Step 3 — Stage and verify changes**

    Run:
    ```bash
    git add -A && git status
    ```

    Verify only these files changed:
    - `frontend/public/env.js` — Railway URL set
    - `docs/demo-walkthrough.md` — Production URL section added

    **Step 4 — Commit and push to main**

    ```bash
    git commit -m "feat(deploy): configure production Railway backend URL and update demo walkthrough"
    git push origin main
    ```

    This triggers:
    - **Vercel auto-deploy** (detects push to main, builds Angular SPA, deploys)
    - **Railway auto-deploy** (detects push to main, builds Docker image, deploys)

    Wait 5-10 minutes for both deployments to complete, then proceed to Task 2.

    **Step 5 — (After deployments finish) Verify health endpoint**

    Once Railway finishes deploying, verify the backend is healthy:
    ```bash
    curl -f https://[your-railway-app].up.railway.app/actuator/health
    ```

    Expected: `{"status":"UP"}`
    If the health check fails, check Railway build logs for errors.
  </action>
  <verify>
    <automated>curl -f https://[your-railway-app].up.railway.app/actuator/health</automated>
  </verify>
  <done>"env.js updated with Railway URL, demo-walkthrough.md updated with production URLs, changes committed and pushed to main. Auto-deploys triggered on Vercel and Railway. Backend health endpoint responds 200."</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke test — run demo walkthrough against production URLs</name>
  <files>
    - "docs/demo-walkthrough.md"
  </files>
  <what-built>
    The agent has:

    1. **Updated `frontend/public/env.js`** with the Railway backend URL — all HTTP requests from the SPA now go to the production backend.

    2. **Updated `docs/demo-walkthrough.md`** — added a "Production Verification" section with live URLs.

    3. **Committed and pushed to `main`** — this triggered auto-deploys on Vercel and Railway.

    4. **Verified the backend health endpoint** responds with `{"status":"UP"}`.

    **Now it's your turn — verify the complete application works in production.**

    Follow the demo walkthrough (`docs/demo-walkthrough.md`) against the production URLs to verify all DEP-01 through DEP-05 criteria.

    **Key things to verify per the success criteria:**

    | # | Requirement | How to Verify |
    |---|-------------|---------------|
    | DEP-01 | Frontend on Vercel | Visit the Vercel URL — app loads without errors |
    | DEP-02 | Backend on Railway | `GET /actuator/health` returns 200 |
    | DEP-03 | Production Docker | Backend build uses multi-stage Dockerfile (check Railway build logs for stage names) |
    | DEP-04 | CI/CD gates | Check GitHub → latest commit — all CI checks passed before merge |
    | DEP-05 | Smoke tests | Full demo walkthrough succeeds |

    **Walkthrough steps to follow from `docs/demo-walkthrough.md`:**

    1. **Login** — Navigate to Vercel URL, click Login, enter `admin@shiftleft.com` / `ShiftLeft!2026`
    2. **Knowledge Base** — Search for "password", verify results appear with highlighted snippets
    3. **AI Chat** — Ask "How do I reset my password?", verify streamed response with sources
    4. **Escalate** — Create a ticket via the escalation flow
    5. **Agent Dashboard** — View and claim the ticket in the agent queue
    6. **Resolve** — Add work note, resolve the ticket with KCS flag
    7. **KCS Draft** — Navigate to Admin → KCS Drafts, verify auto-drafted article exists
    8. **Admin** — Verify user management, tag management pages load
    9. **Language Switch** — Toggle to French, verify all pages render without layout issues

    **Recording results:**

    Keep track of what works and what doesn't. If any single step fails, diagnose and note the issue before proceeding. Common issues:
    - **CORS error**: Check that `APP_CORS_ALLOWED_ORIGINS` in Railway matches your Vercel URL
    - **401 on API calls**: Check the JWT secret is set consistently
    - **AI chat fails**: Check `SPRING_AI_OPENAI_API_KEY` is valid
    - **No seed data**: Check Railway logs for seeder execution
    - **404 on routes**: Vercel should handle SPA routing automatically — if not, check for 404s
  </what-built>
  <how-to-verify>
    Visit the production URLs and follow the demo walkthrough steps. For each step, confirm the expected behavior.

    **Quick verification checklist:**

    1. ✅ Frontend loads at Vercel URL — landing page renders
    2. ✅ Login works — admin credentials accepted
    3. ✅ KB search returns results — articles visible
    4. ✅ AI chat streams responses — sources cited
    5. ✅ Escalation creates ticket — ticket number shown
    6. ✅ Agent dashboard loads — ticket visible and claimable
    7. ✅ Resolution works — status changes to RESOLVED
    8. ✅ KCS draft auto-generated — visible in Admin → KCS Drafts
    9. ✅ Language switch works — French labels render without overflow
    10. ✅ Backend health endpoint: `https://[railway-app].up.railway.app/actuator/health` → 200
  </how-to-verify>
  <resume-signal>
    Type "Verified: all DEP criteria passed" if the smoke test succeeds. If any step fails, describe the failure and I'll help diagnose.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → Vercel/Railway | Git push triggers auto-deploy — trust relies on branch protection |
| Browser → Production | End-user HTTPS requests to Vercel (frontend) and Railway (backend) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | E (Elevation) | Git push to main | mitigate | Branch protection requires CI pass before merge (DEP-04). CI runs lint, test, build, Sonar, OWASP. |
| T-21-06 | S (Spoofing) | Production endpoints | accept | Default domains with HTTPS. No custom domain yet per D-06 (deferred). |
| T-21-07 | R (Repudiation) | Manual smoke test | accept | Solo developer project. Manual walkthrough is recorded in this plan's SUMMARY output. Per D-05 precedent (Phase 8 D-08/D-09). |
</threat_model>

<verification>
1. `cd frontend && npm run build` — Frontend builds without errors (simulates Vercel build)
2. `cd backend && ./mvnw verify -B` — Backend builds and tests pass (simulates Railway build)
3. Backend health endpoint responds 200
4. Frontend loads without console errors (CORS, 404s)
5. Demo walkthrough steps 1-9 all pass against production URLs
6. GitHub Actions CI passed on the latest main commit
</verification>

<success_criteria>
1. **DEP-01 ✓** — Frontend Angular SPA is deployed and accessible on Vercel with default domain
2. **DEP-02 ✓** — Backend Spring Boot API + PostgreSQL are deployed and accessible on Railway with default domain, health endpoint responds 200
3. **DEP-03 ✓** — Production Docker configuration verified: multi-stage build, non-root user, HEALTHCHECK, JRE runtime
4. **DEP-04 ✓** — CI/CD pipeline verifies build, test, lint, and security before merge (branch protection + CI quality gates)
5. **DEP-05 ✓** — Deployed application passes full demo walkthrough (login → AI query → escalate → agent resolves → KCS drafts)
6. D-05 respected: Manual post-deploy verification (no automated smoke tests against prod)
7. D-11 respected: No special deploy ordering (frontend/backend auto-deploy independently)
8. Phase 21 goal achieved: Application is deployed to production on Vercel and Railway with passing health checks
</success_criteria>

<output>
After completion, create `.planning/phases/21-production-deployment/21-03-SUMMARY.md`
</output>
