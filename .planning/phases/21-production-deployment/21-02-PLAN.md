---
phase: 21-production-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "docs/pre-deploy-checklist.md"
autonomous: false
requirements: [DEP-01, DEP-02, DEP-05]
user_setup:
  - service: Railway
    why: "Backend Spring Boot API + PostgreSQL database hosting"
    env_vars:
      - name: SPRING_DATASOURCE_URL
        source: "Railway PostgreSQL add-on → Connect tab → DATABASE_URL (convert to JDBC format)"
      - name: APP_JWT_SECRET
        source: "Generate locally: openssl rand -base64 32"
      - name: APP_AI_ENCRYPTION_KEY
        source: "Generate locally: openssl rand -base64 32"
      - name: APP_AI_ENCRYPTION_SALT
        source: "Generate locally: openssl rand -base64 16"
      - name: SPRING_AI_OPENAI_API_KEY
        source: "OpenAI dashboard → API keys"
      - name: APP_CORS_ALLOWED_ORIGINS
        source: "Vercel project URL (from Vercel dashboard after project creation)"
    dashboard_config:
      - task: "Create Railway project from GitHub repo"
        location: "https://railway.app → New Project → Deploy from GitHub repo"
      - task: "Add PostgreSQL add-on"
        location: "Railway dashboard → New → Database → Add PostgreSQL"
      - task: "Create Vercel project from GitHub repo"
        location: "https://vercel.com → Add New → Project"
  - service: Vercel
    why: "Frontend Angular SPA hosting"
    dashboard_config:
      - task: "Create Vercel project from GitHub repo (root: frontend/)"
        location: "https://vercel.com → Add New → Project"
must_haves:
  truths:
    - "Railway project exists with backend deployed from GitHub"
    - "Railway PostgreSQL add-on is provisioned"
    - "All Railway environment variables are set (DB URL, JWT secret, AI key, CORS, etc.)"
    - "Vercel project exists with frontend deployed from GitHub"
    - "Backend health endpoint responds 200"
    - "Frontend loads in browser without CORS errors"
  artifacts:
    - path: "docs/pre-deploy-checklist.md"
      provides: "Step-by-step setup instructions for Railway + Vercel"
      min_lines: 80
    - path: "Railway project (dashboard)"
      provides: "Backend + PostgreSQL hosting"
    - path: "Vercel project (dashboard)"
      provides: "Frontend SPA hosting"
  key_links:
    - from: "Railway dashboard"
      to: "backend/Dockerfile"
      via: "Git-integrated build"
    - from: "Railway PostgreSQL"
      to: "application.properties"
      via: "SPRING_DATASOURCE_URL env var"
    - from: "Vercel project"
      to: "frontend/"
      via: "Git-integrated build"
---

<objective>
**Set up Railway (backend + PostgreSQL) and Vercel (frontend) projects, configure all environment variables, and verify both deploy successfully.**

Purpose: Create the production infrastructure. The user follows the pre-deploy checklist (created in 21-01) to set up Railway and Vercel projects, configure secrets, and verify the initial builds succeed. After this plan, both services are live on their default domains with correct configuration.

Output: Railway project with PostgreSQL + backend deployed, Vercel project with frontend deployed, all env vars configured.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-production-deployment/21-CONTEXT.md
@docs/pre-deploy-checklist.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute pre-deploy checklist — create Railway and Vercel projects</name>
  <files>
    - "docs/pre-deploy-checklist.md"
  </files>
  <what-built>
    The agent has:

    1. **Created `frontend/public/env.js`** — runtime env config that sets `window.__env.apiBaseUrl`. Currently empty string (dev mode). Must be updated with the Railway URL after project creation.

    2. **Created `frontend/src/app/core/http/base-url.interceptor.ts`** — HTTP interceptor that prepends the Railway backend URL to all `/api/` requests when `window.__env.apiBaseUrl` is set.

    3. **Registered the interceptor** in `frontend/src/app/app.config.ts` (before `errorInterceptor`).

    4. **Added `env.js` script** to `frontend/src/index.html` in `<head>`.

    5. **Updated `backend/src/main/resources/application.properties`** — `server.port=${PORT:8080}` for Railway PORT env var support.

    6. **Verified the `backend/Dockerfile`** — multi-stage, non-root user, HEALTHCHECK, JRE runtime, correct jar name. Ready for Railway.

    7. **Verified the CI pipeline** (`.github/workflows/ci.yml`) — quality gates (lint, test, build, Sonar, OWASP) run on every PR/push to main. No deploy steps added per D-04.

    8. **Created `docs/pre-deploy-checklist.md`** — complete step-by-step guide with all exact env var names, Vercel/Railway configuration details, and verification steps.

    **What needs to be done next (by you):**

    Follow the instructions in `docs/pre-deploy-checklist.md` to complete the production infrastructure setup.

    **Summary of the key steps:**

    **Step 1 — Railway Project**
    - Go to https://railway.app → New Project → Deploy from GitHub repo
    - Configure root directory as `backend`
    - Railway auto-detects `backend/Dockerfile` and builds
    - Watch the build logs for success

    **Step 2 — Railway PostgreSQL**
    - Add PostgreSQL add-on in Railway dashboard
    - Copy `DATABASE_URL` and convert to JDBC format

    **Step 3 — Railway Environment Variables**
    - Set all env vars listed in the checklist (DB URL, JWT secret, AI key, etc.)
    - Note the Railway app URL (e.g., `https://[app].up.railway.app`)

    **Step 4 — Vercel Project**
    - Go to https://vercel.com → Add New → Project
    - Import GitHub repo, root directory: `frontend`
    - Vercel auto-detects Angular framework
    - Note the Vercel app URL (e.g., `https://[app].vercel.app`)

    **Step 5 — Update CORS**
    - Update Railway env var `APP_CORS_ALLOWED_ORIGINS` to the Vercel URL

    **Step 6 — Verify**
    - Backend health: `https://[railway-app].up.railway.app/actuator/health` → 200 OK
    - Frontend loads in browser without CORS errors

    **Note the Railway app URL** — you'll need it for the next plan (21-03) where we update `env.js`.
  </what-built>
  <how-to-verify>
    Complete each step from `docs/pre-deploy-checklist.md`. After all steps are done, verify:

    1. **Backend health check:**
       Visit `https://[your-railway-app].up.railway.app/actuator/health` in browser
       Expected: JSON response with `{"status":"UP"}`

    2. **Frontend loads:**
       Visit `https://[your-vercel-app].vercel.app` in browser
       Expected: Knowledge Hub landing page loads without errors

    3. **No CORS errors:**
       Open browser DevTools (F12) → Console tab
       Expected: No CORS-related error messages

    4. **Railway build logs:**
       Check Railway dashboard → Deployments → Latest build log
       Expected: "Build succeeded" with Flyway migrations and seeders executing

    5. **Vercel build logs:**
       Check Vercel dashboard → Deployments → Latest build log
       Expected: "Build Completed" with Angular build output
  </how-to-verify>
  <resume-signal>
    Type "Railway and Vercel deployed" when both projects are live and verified. Include the Railway app URL and Vercel app URL so I can update env.js in the next plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway admin dashboard | Human accesses Railway dashboard with credentials |
| Vercel admin dashboard | Human accesses Vercel dashboard with credentials |
| env var values in dashboards | Secrets (JWT secret, AI key, DB password) stored in platform secret stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | I (Info Disclosure) | Platform dashboards | mitigate | Secrets entered directly in Railway/Vercel dashboards — never committed to git. `.env.example` documents structure without real values per D-07. |
| T-21-04 | S (Spoofing) | Railway/Vercel accounts | accept | Platform account security is managed by Railway/Vercel (2FA available). Solo developer project — risk accepted. |
</threat_model>

<verification>
1. `https://[railway-app].up.railway.app/actuator/health` returns `{"status":"UP"}`
2. `https://[vercel-app].vercel.app` loads Angular SPA without errors
3. Browser dev console shows no CORS errors
4. Railway build logs show Flyway migrations and seeders executing
5. Vercel build logs show successful Angular build
</verification>

<success_criteria>
1. DEP-01 satisfied: Frontend deployed and accessible on Vercel (default domain)
2. DEP-02 satisfied: Backend + PostgreSQL deployed on Railway (default domain), health endpoint responds
3. D-01 respected: Vercel git-integration (no frontend Dockerfile needed)
4. D-02 respected: Railway git-integration (uses backend/Dockerfile)
5. D-03 respected: Railway managed PostgreSQL provisioned
6. D-06 respected: Default *.vercel.app / *.up.railway.app domains
7. D-07 respected: All secrets in platform-native secret stores, not in code
8. D-10 respected: APP_CORS_ALLOWED_ORIGINS set to Vercel URL
9. D-12 respected: Seeders enabled and running on startup
</success_criteria>

<output>
After completion, create `.planning/phases/21-production-deployment/21-02-SUMMARY.md`
</output>
