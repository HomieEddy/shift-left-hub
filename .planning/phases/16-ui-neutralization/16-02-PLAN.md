---
phase: 16-ui-neutralization
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/app/features/landing/landing.component.html
  - frontend/src/app/features/landing/landing.component.ts
  - frontend/src/app/app.html
  - frontend/src/app/app.css
autonomous: true
requirements:
  - UIN-01
  - UIN-02

must_haves:
  truths:
    - "Guest landing page hero uses neutral knowledge-platform messaging — zero IT-specific terms"
    - "Sidebar shows new icon (Lucide book icon) and 'Knowledge Hub' title"
    - "Nav labels display correctly: 'My Requests', 'Queue', 'Drafts'"
    - "ng build compiles without errors"
    - "All existing frontend tests pass"
  key_links:
    - from: "landing.component.html"
      to: "messages.xlf"
      via: "Uses translation keys for all text content"
    - from: "app.html"
      to: "messages.xlf"
      via: "Sidebar text and nav labels use translation keys"
user_setup: []
---

<objective>
Rewrite the guest landing page hero with neutral knowledge-platform copy, update sidebar branding to use the new icon and title, and update navigation labels to match the neutral terminology.

**Purpose:** The public-facing landing page should present a neutral knowledge platform, not an IT support portal. The sidebar should reflect the new brand identity.
**Output:** Updated landing.component.html with neutral hero, updated app.html with new sidebar branding, updated nav labels.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-ui-neutralization/16-CONTEXT.md
@.planning/phases/16-ui-neutralization/PLAN.md

# Files to read
@frontend/src/app/features/landing/landing.component.html
@frontend/src/app/features/landing/landing.component.ts
@frontend/src/app/app.html
@frontend/src/app/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite guest hero section</name>
  <files>
    frontend/src/app/features/landing/landing.component.html
    frontend/src/app/features/landing/landing.component.ts
  </files>
  <action>
    Read the current landing.component.html first, then rewrite the guest hero section (the section shown when user is not authenticated).

    Identify the guest hero section — it typically starts with an `@if (!authService.isAuthenticated())` or similar guard. Replace:

    - **Hero badge:** Replace "Shift-Left Support Platform" badge with "Knowledge Platform" badge using the translation key `landing.hero.badge`
    - **Hero title:** Replace with new neutral title using `landing.hero.title` (e.g., "Find Answers. Share Knowledge.")
    - **Hero subtitle:** Replace with new neutral subtitle using `landing.hero.subtitle`
    - **Hero description:** Replace with new neutral description using `landing.hero.description`
    - **Features section subtitle:** Replace with neutral copy using `landing.features.subtitle`
    - **CTA section:** Replace "Ready to Shift Left?" with new neutral CTA title/desc using `landing.cta.*` keys
    - **Stats section:** Replace IT metrics with neutral ones (answers found, time saved, knowledge coverage) using `landing.stats.*` keys
    - **How it works section:** Replace IT-focused steps with neutral knowledge platform workflow using `landing.how.step*.title/desc` keys

    Make minimal changes to landing.component.ts — only if there's hero-specific logic that references IT concepts. The component should mostly just render the template.

    IMPORTANT: Do NOT modify the signed-in dashboard sections of landing.component.html — those are handled in Plan 16-03.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/app/features/landing/landing.component.html" -Pattern "landing\.hero\.badge"</automated>
    <automated>Select-String -Path "frontend/src/app/features/landing/landing.component.html" -Pattern "Shift-Left Support Platform" -NotMatch | Out-Null</automated>
  </verify>
  <done>
    - Hero badge replaced with neutral "Knowledge Platform"
    - Hero title/subtitle/description using neutral keys
    - Features section subtitle neutralized
    - CTA section neutralized (no "Shift Left" references)
    - Stats section uses neutral metrics
    - How-it-works steps are neutral knowledge platform workflow
    - No IT-specific terms remain in guest hero section
  </done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar branding</name>
  <files>
    frontend/src/app/app.html
    frontend/src/app/app.css
  </files>
  <action>
    Read the current app.html and app.css, then update the sidebar branding:

    - Replace the "SL" monogram icon in the sidebar header with a book/knowledge Lucide icon (e.g., `lucideBookOpen` or `lucideLibrary`)
    - Update sidebar header text to use `app.title` translation key (now shows "Knowledge Hub")
    - Update any sidebar version/footer text if present (use neutral language)

    Check what Lucide icons are already imported in the component. If `lucideBookOpen` isn't imported, add it. The Sidebar component may be in a separate file — search for the sidebar template.

    The sidebar icons are typically in the sidebar component, which may be in:
    - `frontend/src/app/shared/components/sidebar/`
    - Or inline in app.html

    Find the actual sidebar location first.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/app/" -Pattern "BookOpen|Library" -Include "*.html","*.ts" | Select-Object -First 5</automated>
  </verify>
  <done>
    - Sidebar icon replaced with book/knowledge symbol
    - Sidebar header text uses `app.title` translation key
    - Version/footer text neutralized if present
  </done>
</task>

<task type="auto">
  <name>Task 3: Update navigation labels</name>
  <files>
    frontend/src/app/app.html
  </files>
  <action>
    The nav labels should already use translation keys. Verify they resolve to the new neutral values:

    - "My Tickets" → now resolves from `nav.my-tickets` → "My Requests"
    - "Ticket Queue" → now resolves from `nav.ticket-queue` → "Queue"
    - "KCS Drafts" → now resolves from `nav.kcs-drafts` → "Drafts"
    - Agent section header → update to "Resolver" if applicable

    If any nav labels are hardcoded in the template instead of using translation keys, update them to use the keys. Check for any remaining IT-specific text in the navigation.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/app/app.html" -Pattern "My Tickets|Ticket Queue|KCS Drafts" -NotMatch | Out-Null</automated>
  </verify>
  <done>
    - All nav labels use translation keys (no hardcoded text)
    - Agent section header updated to "Resolver" if present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template rendering | Templates render static content with translation key bindings — no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | HTML templates | accept | Templates are source-controlled. No user input modifies template content. |
</threat_model>

<verification>
1. ✅ Guest hero section reads as neutral knowledge platform
2. ✅ Sidebar shows new icon and "Knowledge Hub" title
3. ✅ All nav labels display correctly (EN + FR via translation keys)
4. 🔲 `ng build` compiles without errors
5. 🔲 All existing frontend tests pass
</verification>

<success_criteria>
- Landing page hero: zero IT-specific terms
- Sidebar: new book icon, "Knowledge Hub" title
- Nav labels: "My Requests", "Queue", "Drafts"
- `ng build` passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-neutralization/16-02-SUMMARY.md`
</output>
