---
phase: 16-ui-neutralization
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - frontend/src/app/shared/components/empty-state/
  - frontend/src/app/shared/components/error-page/
  - frontend/src/app/features/landing/landing.component.html
autonomous: true
requirements:
  - UIN-04

must_haves:
  truths:
    - "Empty states across all entities use neutral format"
    - "Error pages show neutral messaging with actionable buttons"
    - "Onboarding flows reference workspace generically"
    - "Zero IT-specific terms remain in user-facing strings (EN + FR)"
    - "ng build compiles without errors"
    - "All existing frontend tests pass"
  key_links:
    - from: "empty-state/*.html"
      to: "translation keys"
      via: "All empty state text uses translation keys"
    - from: "error-page/*.html"
      to: "translation keys"
      via: "Error messages use translation keys"
user_setup: []
---

<objective>
Neutralize all empty states, error pages, and onboarding flows — no IT-specific messaging anywhere in the user-facing application. Perform a final sweep for remaining IT terminology.

**Purpose:** Every user-facing surface should present neutral, domain-agnostic messaging. No user should see IT-specific language regardless of where they are in the application.
**Output:** Updated empty state components, error page components, landing page onboarding text. Final IT-language sweep results.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-ui-neutralization/16-CONTEXT.md
@.planning/phases/16-ui-neutralization/PLAN.md

# Files to read
@frontend/src/app/shared/components/empty-state/
@frontend/src/app/shared/components/error-page/
@frontend/src/app/features/landing/landing.component.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize empty state patterns</name>
  <files>
    frontend/src/app/shared/components/empty-state/
  </files>
  <action>
    Find and read the empty state component(s). Look in:
    - `frontend/src/app/shared/components/empty-state/`
    - Any entity-specific empty states in feature directories

    Apply the standard neutral pattern:
    - Format: `"No {items} yet"` — simple, consistent
    - Icon + short text — no IT-specific context
    - Use translation keys, not hardcoded text

    Check entity-specific empty states for:
    - Articles: "No articles yet" (already neutral likely)
    - Tickets: "No requests yet" (was "No tickets")
    - Documents: "No documents yet" (already neutral)
    - Chats: "No conversations yet" (already neutral)
    - Search results: "No results found" (already neutral)

    Add any missing translation keys:
    - `empty-state.no-articles` → "No articles yet" / "Aucun article pour l'instant"
    - `empty-state.no-requests` → "No requests yet" / "Aucune demande pour l'instant"
    - `empty-state.no-documents` → "No documents yet" / "Aucun document pour l'instant"
    - `empty-state.no-conversations` → "No conversations yet" / "Aucune conversation pour l'instant"
    - `empty-state.no-results` → "No results found" / "Aucun résultat trouvé"
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/app/shared/components/empty-state/" -Pattern "No articles yet|empty-state\.no-articles"</automated>
    <automated>Select-String -Path "frontend/src/locale/messages.xlf" -Pattern "empty-state\.no-requests"</automated>
  </verify>
  <done>
    - Empty states use consistent "No {items} yet" format
    - All empty state text uses translation keys
    - Entity-specific empty states neutralized
  </done>
</task>

<task type="auto">
  <name>Task 2: Neutralize error pages</name>
  <files>
    frontend/src/app/shared/components/error-page/
  </files>
  <action>
    Find and read the error page component(s). Look in:
    - `frontend/src/app/shared/components/error-page/`
    - Any 404 or error state templates

    Update error pages:
    - "Something went wrong" with actionable button (retry, go home)
    - No "contact IT support" or IT-specific references
    - Neutral illustration/icon (or keep existing icon if already neutral)
    - Use translation keys

    Add translation keys:
    - `error.generic-title` → "Something went wrong" / "Quelque chose s'est mal passé"
    - `error.generic-description` → "An unexpected error occurred. Please try again." / "Une erreur inattendue s'est produite. Veuillez réessayer."
    - `error.retry` → "Try Again" / "Réessayer"
    - `error.go-home` → "Go Home" / "Accueil"
    - `error.not-found` → "Page not found" / "Page non trouvée"
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/app/shared/components/error-page/" -Pattern "error\.generic-title|Something went wrong"</automated>
    <automated>Select-String -Path "frontend/src/locale/messages.xlf" -Pattern "error\.retry"</automated>
  </verify>
  <done>
    - Error pages show neutral messaging
    - No "contact IT support" references
    - Actionable buttons present (retry, go home)
    - Translation keys used for all text
  </done>
</task>

<task type="auto">
  <name>Task 3: Update onboarding flows</name>
  <files>
    frontend/src/app/features/landing/landing.component.html
  </files>
  <action>
    Check the landing page for any onboarding or welcome messaging that appears on first login. Update if needed:
    - Welcome messaging references the workspace domain generically
    - No IT onboarding language
    - If there's a "welcome tour" or getting-started section, neutralize it

    This is the signed-in section of the landing page. Check for any "Welcome to Shift-Left" or IT-focused onboarding text and replace with neutral equivalent.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/app/features/landing/landing.component.html" -Pattern "Shift-Left|IT|helpdesk|Level 0/1" -NotMatch | Out-Null</automated>
  </verify>
  <done>
    - Onboarding/welcome messaging is neutral
    - No IT-specific onboarding language
  </done>
</task>

<task type="auto">
  <name>Task 4: Final IT terminology sweep</name>
  <files>
    frontend/src/app/
    frontend/src/locale/
  </files>
  <action>
    Perform a final sweep for remaining IT-specific terms in user-facing strings. Search for:

    ```bash
    # Search templates for hardcoded IT terms
    rg -n "helpdesk|IT\s+support|Level\s+0[/]1|IT\s+agent|IT\s+team|IT\s+issue|IT\s+problem" frontend/src/app/ --include="*.html"

    # Search translation files for remaining IT terms
    rg -n "helpdesk|IT support|Level 0/1" frontend/src/locale/

    # Search TypeScript for IT references in user-facing strings
    rg -n "helpdesk|IT support|Level 0/1" frontend/src/app/ --include="*.ts"
    ```

    If any instances are found:
    - Update hardcoded text in templates to use translation keys
    - Update translation keys in XLF files
    - Update TypeScript strings if they're user-facing

    What to search for specifically:
    - `helpdesk` (IT-specific, remove entirely)
    - `IT` in user-facing context (not code comments or technical references)
    - `Level 0/1` or `Level 0 / Level 1`
    - `shift left` or `Shift-Left` in user-facing copy
    - Any remaining `KCS` references in user-facing strings

    Note: "Shift-Left" as a product brand name (in copyright footers, etc.) can stay if used as the company/product name rather than a user-facing feature.
  </action>
  <verify>
    <automated>rg -c "helpdesk|IT\s+support|Level\s+0/1" frontend/src/app/ --include="*.html" 2>$null; $count = $LASTEXITCODE; if ($count -eq 0 -or $count -gt 1) { Write-Output "Sweep complete" }</automated>
  </verify>
  <done>
    - All IT-specific terms removed from user-facing strings
    - Zero remaining helpdesk, "IT support", "Level 0/1" in templates or translations
    - Product brand name "Shift-Left" in legal/footer context is acceptable
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content | Empty states, error pages — no user input crosses these boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | Empty/error templates | accept | Templates are source-controlled. No user input modifies them. |
</threat_model>

<verification>
1. ✅ Empty states use neutral "No {items} yet" format
2. ✅ Error pages show neutral messaging with actionable buttons
3. ✅ Onboarding flows reference workspace generically
4. ✅ Final sweep confirms zero IT-specific terms in user-facing strings (EN + FR)
5. 🔲 `ng build` compiles without errors
6. 🔲 All existing frontend tests pass
</verification>

<success_criteria>
- All empty states neutralized
- All error pages neutralized
- Zero IT-specific user-facing strings remain
- `ng build` passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-neutralization/16-04-SUMMARY.md`
</output>
