---
phase: 16-ui-neutralization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.html
  - frontend/src/favicon.ico
  - frontend/src/locale/messages.xlf
  - frontend/src/locale/messages.fr.xlf
  - frontend/src/styles.css
autonomous: true
requirements:
  - UIN-03

must_haves:
  truths:
    - "Favicon is replaced with a neutral book/knowledge symbol — no IT imagery"
    - "Page title changed from 'ShiftleftHub' to 'Knowledge Hub'"
    - "Color palette shifted to warm slate/charcoal with a single accent color"
    - "All EN translation keys updated to neutral language (landing hero, nav, chat, kcs, agent terms)"
    - "All FR translation keys mirror the EN changes"
    - "ng build compiles without errors"
  key_links:
    - from: "index.html"
      to: "favicon.ico"
      via: "favicon link in <head>"
    - from: "messages.xlf"
      to: "messages.fr.xlf"
      via: "Mirror all EN key changes in FR"
user_setup: []
---

<objective>
Establish the neutral brand foundation — replace favicon, update page title, shift color palette to warm slate/charcoal, and update all EN/FR translation keys to remove IT-specific terminology. Everything else in this phase depends on this foundation.

**Purpose:** Remove all IT-specific branding from the visual and textual foundation of the application. The new brand is a warm, neutral knowledge-platform identity.
**Output:** Updated favicon.ico, index.html, color palette in styles.css, and all translation key changes in messages.xlf + messages.fr.xlf.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-ui-neutralization/16-CONTEXT.md
@.planning/phases/16-ui-neutralization/PLAN.md

# Files to read
@frontend/src/index.html
@frontend/src/locale/messages.xlf
@frontend/src/locale/messages.fr.xlf
@frontend/src/styles.css
@frontend/tailwind.config.js or @frontend/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace favicon</name>
  <files>
    frontend/src/favicon.ico
    frontend/src/index.html
  </files>
  <action>
    Create a new favicon.ico with a neutral book/knowledge symbol (no IT imagery). The favicon should be a simple, clean knowledge icon — an open book, a lightbulb, or an abstract knowledge mark.

    Since we can't generate binary .ico files directly, use an SVG approach:
    1. Create `frontend/src/favicon.svg` as the primary favicon (modern browsers)
    2. Update `index.html` to reference the SVG favicon in addition to the .ico fallback

    The SVG should be a simple, clean design — an open book icon in a warm slate color, with transparent background, 32x32 viewBox.

    Update `index.html`:
    - Change `<title>ShiftleftHub</title>` to `<title>Knowledge Hub</title>` (neutral name)
    - Add `<link rel="icon" type="image/svg+xml" href="favicon.svg">`
    - Keep the existing favicon.ico reference as fallback
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/index.html" -Pattern "<title>Knowledge Hub</title>"</automated>
    <automated>Test-Path "frontend/src/favicon.svg"</automated>
  </verify>
  <done>
    - favicon.svg created with neutral book icon
    - index.html title updated to "Knowledge Hub"
    - Favicon references in index.html point to new assets
  </done>
</task>

<task type="auto">
  <name>Task 2: Shift color palette</name>
  <files>
    frontend/src/styles.css
  </files>
  <action>
    Update the CSS custom properties in styles.css (or the global styles file) to shift from the current indigo/blue tech-primary to a warm slate/charcoal scheme:

    New palette:
    - `--color-primary-50` → `#f8fafc` (slate 50)
    - `--color-primary-100` → `#f1f5f9` (slate 100)
    - `--color-primary-200` → `#e2e8f0` (slate 200)
    - `--color-primary-300` → `#cbd5e1` (slate 300)
    - `--color-primary-400` → `#94a3b8` (slate 400)
    - `--color-primary-500` → `#64748b` (slate 500)
    - `--color-primary-600` → `#475569` (slate 600)
    - `--color-primary-700` → `#334155` (slate 700)
    - `--color-primary-800` → `#1e293b` (slate 800)
    - `--color-primary-900` → `#0f172a` (slate 900)

    Accent color (single warm accent):
    - `--color-accent` → `#d97706` (amber 600 — warm, inviting)
    - `--color-accent-hover` → `#b45309` (amber 700)

    Surface colors:
    - `--color-surface-primary` → `#ffffff`
    - `--color-surface-secondary` → `#f8fafc`
    - `--color-surface-tertiary` → `#f1f5f9`

    Text colors:
    - `--color-text-primary` → `#1e293b` (slate 800)
    - `--color-text-secondary` → `#475569` (slate 600)
    - `--color-text-muted` → `#94a3b8` (slate 400)

    Ensure all changes maintain WCAG AA contrast ratios. The primary-500 (#64748b) passes AA on white backgrounds for body text; primary-700 (#334155) passes AAA.

    First, read the existing styles.css to see the current custom properties structure, then modify accordingly.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/styles.css" -Pattern "--color-primary-500:\s*#64748b"</automated>
    <automated>Select-String -Path "frontend/src/styles.css" -Pattern "--color-accent:\s*#d97706"</automated>
  </verify>
  <done>
    - Color palette updated to warm slate/charcoal with amber accent
    - All CSS custom properties updated consistently
    - WCAG AA contrast maintained
  </done>
</task>

<task type="auto">
  <name>Task 3: Update all EN translation keys</name>
  <files>
    frontend/src/locale/messages.xlf
  </files>
  <action>
    Update all EN translation keys in messages.xlf to remove IT-specific terminology. Read the file first to find the exact trans-unit IDs and source text, then update them.

    Key changes per 16-CONTEXT.md decisions:
    - `app.title` → "Knowledge Hub" (was "SL Knowledge Hub")
    - `landing.hero.badge` → "Knowledge Platform" (was "Shift-Left Support Platform")
    - `landing.hero.title` → "Find Answers. Share Knowledge." (was "Shift-Left Knowledge Hub")
    - `landing.hero.subtitle` → "Your intelligent knowledge platform for every team" (was "Resolve issues before they reach the queue")
    - `landing.hero.description` → "Empower your teams with an AI-powered knowledge base that captures answers, reduces repeat questions, and makes expertise accessible to everyone." (was IT-focused)
    - `landing.features.subtitle` → "Built for teams that value knowledge" (was "Built for IT teams...")
    - `landing.cta.title` → "Start Building Your Knowledge Base" (was "Ready to Shift Left?")
    - `landing.cta.desc` → "Create a workspace, invite your team, and start capturing knowledge that works for everyone." (was IT-focused)
    - `landing.how.step*.title/desc` — neutral step descriptions
    - `landing.stats.*` — neutral metrics (answers found, time saved, knowledge coverage)
    - `nav.my-tickets` → "My Requests"
    - `nav.ticket-queue` → "Queue"
    - `nav.kcs-drafts` → "Drafts"
    - `chat.subtitle` → "Ask me anything about your workspace"
    - `chat.input-placeholder` → "Ask a question..."
    - All `kcs.*` keys — remove KCS terminology, use "Draft" or "Auto-Draft"
    - All `agent.*` keys — update agent/role references to "resolver" or "team member"

    For each trans-unit:
    - Update the `<source>` text
    - Keep the same `id` attribute
    - Do NOT add or remove any trans-units
    - Do NOT change the structure of the XLF file

    Read the actual file first to find the exact trans-unit IDs and current source text.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/locale/messages.xlf" -Pattern "Knowledge Hub"</automated>
    <automated>Select-String -Path "frontend/src/locale/messages.xlf" -Pattern "My Requests"</automated>
    <automated>Select-String -Path "frontend/src/locale/messages.xlf" -Pattern "Shift-Left Support Platform" -NotMatch | Out-Null; $? -and (Select-String -Path "frontend/src/locale/messages.xlf" -Pattern "Resolve issues before they reach the queue" -NotMatch)</automated>
  </verify>
  <done>
    - All landing hero/badge/title/subtitle/description keys updated to neutral
    - All features/CTA/stats keys updated to neutral
    - All how-it-works step keys updated
    - nav.my-tickets → "My Requests"
    - nav.ticket-queue → "Queue"
    - nav.kcs-drafts → "Drafts"
    - chat subtitle/placeholder updated
    - kcs.* keys neutralized
    - agent.* keys updated to "resolver"/"team member"
  </done>
</task>

<task type="auto">
  <name>Task 4: Mirror all changes in messages.fr.xlf</name>
  <files>
    frontend/src/locale/messages.fr.xlf
  </files>
  <action>
    Mirror all the changes from messages.xlf in the French translation file messages.fr.xlf.

    For each trans-unit that was changed in messages.xlf:
    - Update the `<source>` text to match English (the source is always English per Angular i18n convention)
    - Update the `<target>` with the French translation

    French translations:
    - `app.title` → "Knowledge Hub" (keep as-is, brand name)
    - `landing.hero.badge` → "Plateforme de Connaissances"
    - `landing.hero.title` → "Trouvez des Réponses. Partagez le Savoir."
    - `landing.hero.subtitle` → "Votre plateforme de connaissances intelligente pour chaque équipe"
    - `landing.hero.description` → "Donnez à vos équipes les moyens d'agir avec une base de connaissances alimentée par l'IA qui capture les réponses, réduit les questions répétitives et rend l'expertise accessible à tous."
    - `landing.features.subtitle` → "Conçu pour les équipes qui valorisent le savoir"
    - `landing.cta.title` → "Commencez à Construire Votre Base de Connaissances"
    - `landing.cta.desc` → "Créez un espace de travail, invitez votre équipe et commencez à capturer des connaissances qui profitent à tous."
    - `nav.my-tickets` → "Mes Demandes"
    - `nav.ticket-queue` → "File d'Attente"
    - `nav.kcs-drafts` → "Brouillons"
    - `chat.subtitle` → "Demandez-moi n'importe quoi sur votre espace de travail"
    - `chat.input-placeholder` → "Posez une question..."

    Read the actual file first to find the exact trans-unit IDs and current translations.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/locale/messages.fr.xlf" -Pattern "Mes Demandes"</automated>
    <automated>Select-String -Path "frontend/src/locale/messages.fr.xlf" -Pattern "File d'Attente"</automated>
  </verify>
  <done>
    - All FR translation keys updated to mirror EN changes
    - French translations are natural and appropriate
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static assets | Favicon, styles, translations — no user input crosses these boundaries. Changes are purely presentational. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Translation keys | accept | Translation key changes are source-controlled. No user input modifies XLF files. |
</threat_model>

<verification>
1. ✅ favicon.svg created with neutral book icon
2. ✅ index.html title updated
3. ✅ Color palette updated in styles.css
4. ✅ EN translation keys neutralized
5. ✅ FR translation keys mirrored
6. 🔲 `ng build` compiles without errors
</verification>

<success_criteria>
- favicon.svg exists as a neutral knowledge symbol
- Page title reads "Knowledge Hub"
- Color palette is warm slate/charcoal with amber accent
- All IT-specific translation keys are neutralized in EN and FR
- `ng build` passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-neutralization/16-01-SUMMARY.md`
</output>
