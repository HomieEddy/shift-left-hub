---
phase: 18-unit-test-tightening
plan: 05
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
  - 18-03
  - 18-04
files_modified:
  - backend/pom.xml
autonomous: true
requirements:
  - TST-06
  - TST-07
must_haves:
  truths:
    - "JaCoCo thresholds updated from 40%/30% to 60%/50% line/branch coverage"
    - "mvn verify passes with new thresholds — coverage gap analysis resolved by Plans 01-04"
    - "pnpm test -- --run passes — all 180+ frontend tests pass"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Updated JaCoCo thresholds at 60%/50%"
      contains: "0.60"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline that gates on the new coverage thresholds"
  key_links:
    - from: "pom.xml JaCoCo config"
      to: "mvn verify"
      via: "Coverage check runs during verify phase"
      pattern: "minimum>0.6"
---

<objective>
Update JaCoCo coverage thresholds from current 40%/30% to 60%/50% line/branch coverage and run full end-to-end verification of both backend and frontend test suites.

Purpose: D-05 mandates incrementally raising thresholds from Phase 8's initial 40%/30% to 60%/50%. This is the final plan in the phase because the expanded test coverage from Plans 01-04 must first exist for the build to pass the new thresholds.

Output: Updated pom.xml with new thresholds. Full verification that `mvn verify` and `pnpm test -- --run` both pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AGENTS.md
@.planning/phases/18-unit-test-tightening/18-CONTEXT.md
@.planning/phases/18-unit-test-tightening/18-01-SUMMARY.md
@.planning/phases/18-unit-test-tightening/18-02-SUMMARY.md
@.planning/phases/18-unit-test-tightening/18-03-SUMMARY.md
@.planning/phases/18-unit-test-tightening/18-04-SUMMARY.md
@backend/pom.xml
@.github/workflows/ci.yml

<interfaces>
<!-- Current JaCoCo configuration in pom.xml -->

```xml
<!-- Line 279-330 of backend/pom.xml -->
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.12</version>
    <executions>
        <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
        </execution>
        <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
        </execution>
        <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
                <rules>
                    <rule>
                        <element>BUNDLE</element>
                        <limits>
                            <limit>
                                <counter>LINE</counter>
                                <value>COVEREDRATIO</value>
                                <minimum>0.40</minimum>  <!-- UPDATE TO 0.60 -->
                            </limit>
                            <limit>
                                <counter>BRANCH</counter>
                                <value>COVEREDRATIO</value>
                                <minimum>0.30</minimum>  <!-- UPDATE TO 0.50 -->
                            </limit>
                        </limits>
                    </rule>
                </rules>
                <excludes>
                    <exclude>**/api/dto/*</exclude>
                    <exclude>**/domain/*</exclude>
                    <exclude>**/*Repository.*</exclude>
                    <exclude>**/config/*</exclude>
                    <exclude>**/*Application.*</exclude>
                </excludes>
            </configuration>
        </execution>
    </executions>
</plugin>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update JaCoCo coverage thresholds in pom.xml</name>
  <files>backend/pom.xml</files>
  <action>
    Update the JaCoCo coverage threshold values in `backend/pom.xml`.

    Change:
    - LINE minimum from `0.40` to `0.60` (60% line coverage per D-05)
    - BRANCH minimum from `0.30` to `0.50` (50% branch coverage per D-05)

    The exact block to modify (around lines 303-312):
    ```xml
    <limit>
        <counter>LINE</counter>
        <value>COVEREDRATIO</value>
        <minimum>0.40</minimum>
    </limit>
    <limit>
        <counter>BRANCH</counter>
        <value>COVEREDRATIO</value>
        <minimum>0.30</minimum>
    </limit>
    ```
    Change to 0.60 and 0.50 respectively.

    Do NOT change:
    - JaCoCo version (keep 0.8.12)
    - Excludes (DTOs, domain entities, repositories, config, Application — these are intentionally excluded per Phase 8 D-04 exclusion design)
    - Any other plugin configuration

    Per D-06: Coverage thresholds apply project-wide (not per-module) — no per-module tuning.
  </action>
  <verify>
    <automated>cd backend && mvn verify -B -q 2>&1 | tail -30</automated>
  </verify>
  <done>JaCoCo thresholds updated to 60%/50%. `mvn verify` passes with all backend tests (unit + integration) and coverage checks.</done>
</task>

<task type="auto">
  <name>Task 2: Run full backend test suite + coverage verification</name>
  <files>backend/pom.xml</files>
  <action>
    Run the full backend verification pipeline to ensure the new thresholds pass:
    ```bash
    mvn clean verify -B
    ```

    This runs:
    1. `mvn compile` — compilation check
    2. `mvn test` — all unit tests (Plans 01-02 expanded coverage)
    3. `mvn verify` — integration tests + JaCoCo report + JaCoCo check with new 60%/50% thresholds
    4. Checkstyle + SpotBugs (configured in verify phase)

    If the JaCoCo check fails (threshold not met), investigate:
    - Which modules/services have low coverage
    - Whether any test files from Plans 01-02 missed edge cases
    - Add targeted tests to the low-coverage areas
    
    If coverage is just below threshold (e.g., 57%), adjust thresholds temporarily down to the achievable level AND report the gap. But 60%/50% is carefully chosen per D-05 as achievable — if Thresholds consistently fail at 60%/50%, lower to 55%/45% and document why.

    Record the final coverage percentages for the SUMMARY.md.
  </action>
  <verify>
    <automated>cd backend && mvn clean verify -B 2>&1 | tail -40</automated>
  </verify>
  <done>`mvn clean verify -B` passes with zero errors. Checkstyle: 0, SpotBugs: 0, tests: all pass, JaCoCo: ≥60% line, ≥50% branch.</done>
</task>

<task type="auto">
  <name>Task 3: Run full frontend test suite</name>
  <files></files>
  <action>
    Run the complete frontend test suite (includes specs from Plans 03-04):
    ```bash
    pnpm test -- --run
    ```

    Verify:
    - All existing tests still pass (Plans 03-04 added new tests, didn't break existing)
    - No new flaky tests introduced
    - Total test count has increased from 127 to ~180+ (Plans 03-04 added ~50-60 new tests across 11 spec files)

    Run lint check as well:
    ```bash
    pnpm lint
    ```

    Run build check:
    ```bash
    pnpm build
    ```

    Record the final test counts per component for the SUMMARY.md.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>`pnpm test -- --run` passes with all tests. `pnpm lint` passes. `pnpm build` passes. Total frontend tests > 170.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Coverage gate | JaCoCo coverage check is a hard fail in verify phase — blocks builds below threshold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | E: Elevation | Coverage config | mitigate | Thresholds set via pom.xml; review before commit — 60%/50% target per D-05 |
| T-18-09 | T: Tampering | CI pipeline | accept | CI gates on verify phase; thresholds enforced per merge |
</threat_model>

<verification>
1. `mvn clean verify -B` — all backend tests pass, Checkstyle 0, SpotBugs 0, JaCoCo ≥60%line/≥50%branch
2. `pnpm test -- --run` — all frontend tests pass
3. `pnpm lint` — zero errors
4. `pnpm build` — clean build
5. JaCoCo thresholds in pom.xml confirmed updated to 0.60/0.50
</verification>

<success_criteria>
- JaCoCo thresholds updated from 40%/30% to 60%/50% in pom.xml
- `mvn clean verify -B` passes (all 50+ backend tests, coverage check, Checkstyle, SpotBugs)
- `pnpm test -- --run` passes (all 180+ frontend tests)
- `pnpm lint` passes (zero errors)
- `pnpm build` passes (clean build)
- Phase 18 success criteria met: TST-06 and TST-7 both satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/18-unit-test-tightening/18-05-SUMMARY.md`
</output>
