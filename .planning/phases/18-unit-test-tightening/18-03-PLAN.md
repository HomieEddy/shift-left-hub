---
phase: 18-unit-test-tightening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/features/agent/agent-ticket-detail/agent-ticket-detail.component.spec.ts
  - frontend/src/app/features/agent/agent-ticket-list/agent-ticket-list.component.spec.ts
  - frontend/src/app/features/landing/landing.component.spec.ts
autonomous: true
requirements:
  - TST-07
must_haves:
  truths:
    - "Agent ticket detail smart component has unit tests covering load, claim, add work note, resolve, error handling, and i18n labels"
    - "Agent ticket list smart component has unit tests covering load, filter by status/category/urgency, search with debounce, claim flow, empty states, and error handling"
    - "Landing component has unit tests covering render, workspace switcher integration, and i18n labels"
  artifacts:
    - path: "frontend/src/app/features/agent/agent-ticket-detail/agent-ticket-detail.component.spec.ts"
      provides: "Agent ticket detail smart component tests"
      exists: true
    - path: "frontend/src/app/features/agent/agent-ticket-list/agent-ticket-list.component.spec.ts"
      provides: "Agent ticket list smart component tests"
      exists: true
    - path: "frontend/src/app/features/landing/landing.component.spec.ts"
      provides: "Landing page smart component tests"
      exists: true
  key_links:
    - from: "AgentTicketDetailComponent"
      to: "AgentTicketService"
      via: "Object literal mock with vi.fn() — inject via TestBed providers"
      pattern: "provide: AgentTicketService, useValue: mock"
    - from: "AgentTicketListComponent"
      to: "AgentTicketService"
      via: "Object literal mock, Subject for observable control"
      pattern: "Subject"
---

<objective>
Add unit tests for the two highest-priority untested frontend smart components (agent-ticket-detail, agent-ticket-list per D-02) plus the landing page component.

Purpose: TST-07 requires meaningful smart component coverage. Per D-02, agent features are prioritized as the most user-facing untested area. The landing page is the app's entry point and should verify basic render and integration.

Output: 3 new spec files with complete test suites for AgentTicketDetailComponent, AgentTicketListComponent, and LandingComponent.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AGENTS.md
@.planning/phases/18-unit-test-tightening/18-CONTEXT.md
@frontend/package.json

<interfaces>
<!-- Existing frontend test patterns extracted from ticket-list.component.spec.ts and chat.component.spec.ts -->

Object-literal service mock pattern (from ticket-list.component.spec.ts):
```typescript
let ticketService: { getTickets: ReturnType<typeof vi.fn> };
// ...
ticketService = {
  getTickets: vi.fn().mockReturnValue(of(mockTickets)),
};
await TestBed.configureTestingModule({
  imports: [TicketListComponent],
  providers: [{ provide: TicketService, useValue: ticketService }, provideRouter([])],
}).compileComponents();
```

Subject for loading state testing:
```typescript
const pendingSubject = new Subject<unknown>();
ticketService.getTickets.mockReturnValue(pendingSubject.asObservable());
component.loadTickets();
expect(component.isLoading()).toBe(true);
pendingSubject.next(mockData);
```

Error testing:
```typescript
const errorSubject = new Subject<unknown>();
errorSubject.error(new Error('Failed'));
// Component should handle error gracefully with null/empty data
```

afterEach verify pattern:
```typescript
afterEach(() => {
  httpMock.verify();
});
```

Key component interfaces (read before implementing):

AgentTicketDetailComponent signals/methods:
- ticket = signal<AgentTicket | null>(null)
- workNotes = signal<WorkNote[]>([])
- isLoading = signal(true), isError = signal(false)
- claimError, resolveError, workNoteError = signal<string | null>(null)
- contextExpanded, transcriptExpanded, sourcesExpanded — collapse signals
- newWorkNote = signal(''), resolutionNotes = signal(''), isKnowledgeGap = signal(false)
- isSubmittingNote, isResolving, isClaiming = signal(false)
- resolveConfirmOpen = signal(false)
- parsedContext = computed(() => ...), transcriptMessages = computed(() => ...)
- loadTicket(id), loadWorkNotes(id), addWorkNote(), claimTicket(), confirmResolve()
- Uses AgentTicketService: getTicket, addWorkNote, claimTicket, resolveTicket, getWorkNotes
- Uses TranslationService for all labels (computed properties)

AgentTicketListComponent signals/methods:
- tickets = signal<AgentTicket[]>([]), filteredTickets = signal([])
- isLoading, isError signals
- activeStatus = signal('ALL'), selectedCategory, selectedUrgency, searchQuery
- searchSubject = new Subject<string>() with debounceTime(300) + distinctUntilChanged
- claimConfirmOpen, claimingTicketId, claimError signals
- loadTickets(), onSearchInput(value), changeStatusTab(status), onCategoryChange(), onUrgencyChange()
- openClaimConfirm(id), confirmClaim(), cancelClaim()
- Uses AgentTicketService: getTickets(filters), claimTicket(id)
- Uses TranslationService for labels (computed properties)
- Uses Router for navigation after claim

LandingComponent signals/methods:
- Simple landing page with welcome message, workspace switcher integration
- Uses TranslationService for labels
- No complex business logic — primarily presentational with i18n
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AgentTicketDetailComponent tests</name>
  <files>frontend/src/app/features/agent/agent-ticket-detail/agent-ticket-detail.component.spec.ts</files>
  <behavior>
    - Component loads ticket and work notes on init from route param
    - Load ticket shows loading state then displays data
    - Error loading ticket shows error state and preserves retry
    - Claim ticket calls service, updates ticket on success, shows error on failure
    - Add work note with empty content does nothing, with content calls service
    - Resolve ticket opens confirm dialog, confirm calls service, cancel closes dialog
    - parsedContext computed from shiftLeftContext JSON
    - i18n computed labels call TranslationService
  </behavior>
  <action>
    Create `agent-ticket-detail.component.spec.ts` following the existing pattern from `ticket-list.component.spec.ts` (object-literal mocks with `vi.fn()`, `Subject` for async control, `TestBed` for component fixture).

    Dependencies: AgentTicketService, TranslationService, ActivatedRoute (paramMap for ticket ID), Router (used by component? Check — detail component may not navigate)

    ```typescript
    import { ComponentFixture, TestBed } from '@angular/core/testing';
    import { provideRouter } from '@angular/router';
    import { of, Subject } from 'rxjs';
    import { vi } from 'vitest';
    import { AgentTicketDetailComponent } from './agent-ticket-detail.component';
    import { TranslationService } from '../../../core/i18n/translation.service';
    import { AgentTicketService } from '../agent-ticket.service';
    ```

    Mock AgentTicketService with methods: getTicket, getWorkNotes, addWorkNote, claimTicket, resolveTicket
    Mock TranslationService with translate method returning vi.fn(() => 'translated')
    Mock ActivatedRoute with snapshot.paramMap.get returning a ticket ID

    Test cases:
    1. `should create` — component instantiation
    2. `should load ticket and work notes on init` — ngOnInit loads from route param, both services called
    3. `should show loading and loaded states` — ticket load → isLoading goes true → then false
    4. `should handle ticket load error gracefully` — service error → isError set, error not thrown
    5. `should handle work notes load error gracefully` — work note error sets workNoteError, doesn't crash
    6. `should call claimTicket on claim` — claim button handler calls service claim method
    7. `should handle claim error gracefully` — claim failure sets claimError, doesn't crash
    8. `should add work note with content` — addWorkNote called with trimmed content, note cleared on success
    9. `should not add work note with empty content` — empty/whitespace input skipped
    10. `should open and confirm resolve dialog` — confirmResolve calls service resolveTicket
    11. `should cancel resolve dialog without resolving` — cancelResolveConfirm closes dialog
    12. `should parse shiftLeftContext JSON` — valid JSON → parsedContext returns object, invalid → null
    13. `should translate labels via TranslationService` — computed label signals call translate()

    Follow D-10/D-11 patterns:
    - Object literal service mocks with vi.fn()
    - Subject for observable control in loading/error states
    - TestBed.createComponent + fixture.detectChanges()
    - No real HTTP calls
    - No trivial "component renders" tests without assertions (per D-08)
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run --reporter=verbose src/app/features/agent/agent-ticket-detail/agent-ticket-detail.component.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>13+ tests pass for AgentTicketDetailComponent. Loading, error, claim, work note, resolve, and i18n scenarios covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AgentTicketListComponent tests</name>
  <files>frontend/src/app/features/agent/agent-ticket-list/agent-ticket-list.component.spec.ts</files>
  <behavior>
    - Component loads tickets on init
    - Filter by status tab changes filtered tickets
    - Filter by category selector reloads tickets
    - Filter by urgency selector reloads tickets
    - Search input debounces before triggering load
    - Claim confirmation dialog opens and confirms
    - Claim navigates to detail on success
    - Error loading tickets shows error state
    - Empty ticket list shows... 
    - i18n label computed properties
  </behavior>
  <action>
    Create `agent-ticket-list.component.spec.ts` following the same object-literal mock pattern.

    Dependencies: AgentTicketService, Router (for navigation after claim), TranslationService

    Mock AgentTicketService with: getTickets(filters), claimTicket(id)
    Mock Router with: navigate (vi.fn())
    Mock TranslationService with: translate returns 'translated'

    Test data: Array of mock AgentTicket objects with various statuses, categories, urgencies.

    Test cases:
    1. `should create` — component instantiation
    2. `should load tickets on init` — getTickets called with default filters, tickets signal populated
    3. `should filter by status tab` — changeStatusTab('IN_PROGRESS') calls getTickets with status filter
    4. `should change category filter and reload` — onCategoryChange with selected value
    5. `should change urgency filter and reload` — onUrgencyChange with selected value
    6. `should debounce search input` — onSearchInput fires searchSubject, getTickets called with search param
    7. `should retry load after error` — error → retry state → loadTickets called again
    8. `should open claim confirmation dialog` — openClaimConfirm sets claimingTicketId + opens dialog
    9. `should confirm claim and navigate to detail` — confirmClaim calls service + Router.navigate
    10. `should cancel claim without navigating` — cancelClaim resets state, no navigation
    11. `should handle claim error gracefully` — claim failure sets claimError
    12. `should handle ticket load error gracefully` — error response sets isError
    13. `should show empty state when no tickets` — empty array returns, no crash
    14. `should translate labels` — computed label properties call translate()

    Follow established patterns from ticket-list.component.spec.ts for filter testing, loading states, error handling.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run --reporter=verbose src/app/features/agent/agent-ticket-list/agent-ticket-list.component.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>14+ tests pass for AgentTicketListComponent. Filtering, search, claim flow, error handling, and i18n covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create LandingComponent tests</name>
  <files>frontend/src/app/features/landing/landing.component.spec.ts</files>
  <behavior>
    - Component renders without errors
    - Uses TranslationService for labels
    - Redirects to workspace if user has one
    - Displays welcome message
  </behavior>
  <action>
    Create `landing.component.spec.ts`.

    Dependencies: AuthService (for current user check), Router, TranslationService, WorkspaceService (for workspace list)

    Mock pattern: Object-literal mock for each service with vi.fn().

    Test cases:
    1. `should create` — component instantiation
    2. `should display welcome heading` — translation service called for welcome key
    3. `should show workspace list when user has workspaces` — workspaces > 0 → list shown
    4. `should show empty state when no workspaces` — empty workspace list handled gracefully
    5. `should navigate to workspace on select` — clicking workspace calls Router.navigate
    6. `should translate all labels` — computed label properties call translate()
    7. `should handle auth error gracefully` — auth check fails, component doesn't crash

    Note: Keep tests concise — landing is a simpler component with less business logic than agent components. Focus on render, i18n, and the workspace selection flow.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run --reporter=verbose src/app/features/landing/landing.component.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>7+ tests pass for LandingComponent. Render, i18n, workspace selection, and auth check covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Service API | All HTTP calls mocked via object-literal mocks or HttpTestingController |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | T: Tampering | Service mock setup | mitigate | Verify mock is called with expected params; use `toHaveBeenCalledWith()` |
| T-18-05 | D: DoS | Error in component | mitigate | All error paths tested — errors set error signals, don't crash component tree |
</threat_model>

<verification>
1. All 3 spec files compile: `pnpm build` or `pnpm test` compile step
2. All new tests pass: `pnpm test -- --run`
3. No real HTTP calls — all services mocked
4. No trivial tests — every test asserts meaningful behavior
5. Follow existing patterns (object-literal mocks, Subjects for loading/error states)
</verification>

<success_criteria>
- AgentTicketDetailComponent: 13+ tests covering load, claim, work notes, resolve, error, i18n
- AgentTicketListComponent: 14+ tests covering filters, search, claim flow, error, i18n
- LandingComponent: 7+ tests covering render, i18n, workspace selection
- `pnpm test -- --run` passes — all frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-unit-test-tightening/18-03-SUMMARY.md`
</output>
