---
phase: 18-unit-test-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/shiftleft/hub/user/service/AuthServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/ticket/service/TicketServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/article/service/ArticleServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/category/service/CategoryServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/tag/service/TagServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/workspace/service/WorkspaceServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/kcs/service/KcsDraftingServiceTest.java
  - backend/src/test/java/com/shiftleft/hub/common/config/HrSeederTest.java
  - backend/src/test/java/com/shiftleft/hub/common/config/MasterSeederTest.java
autonomous: true
requirements:
  - TST-06
must_haves:
  truths:
    - "Existing backend unit tests have additional edge-case coverage for null/empty inputs, workspace isolation, duplicate detection, not-found scenarios, and authorization mismatches"
    - "Every existing test file has at least 2-4 new test methods covering boundary conditions"
    - "No trivial tests added — all new tests verify meaningful business logic"
  artifacts:
    - path: "backend/src/test/java/com/shiftleft/hub/user/service/AuthServiceTest.java"
      provides: "Expanded auth service edge-case tests"
      min_lines: 200
    - path: "backend/src/test/java/com/shiftleft/hub/ticket/service/TicketServiceTest.java"
      provides: "Expanded ticket service edge-case tests"
      min_lines: 285
    - path: "backend/src/test/java/com/shiftleft/hub/kcs/service/KcsDraftingServiceTest.java"
      provides: "Expanded KCS drafting service edge-case tests"
      min_lines: 400
  key_links:
    - from: "Test files"
      to: "Corresponding service implementations"
      via: "@InjectMocks + @Mock for repository/dependency injection"
      pattern: "@InjectMocks"
---

<objective>
Deepen existing 18 backend unit test files with edge-case coverage — null/empty inputs, authorization mismatches, not-found scenarios, workspace isolation violations, duplicate detection, and boundary conditions.

Purpose: TST-06 requires tightening backend unit tests with meaningful coverage. Rather than just adding new test files, deepen all existing 18 test files with the edge cases they're missing per D-03 and D-07.
Output: Each of the 18 existing backend unit test files gains 2-4 new test methods covering boundary conditions from D-07.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/AGENTS.md
@.planning/phases/18-unit-test-tightening/18-CONTEXT.md
@backend/pom.xml
@backend/src/test/java/com/shiftleft/hub/

<interfaces>
<!-- Key patterns extracted from existing tests -->

From AuthServiceTest.java:
- Pattern: @ExtendWith(MockitoExtension.class), @Mock dependencies, @InjectMocks service
- Factory: private User createUser() { return User.builder()... }
- Section headers: // ── methodName ─────────────────────────────────────
- Verify patterns: verify(repo).save(any(Target.class)), verify(repo, never()).save(any())

From TicketServiceTest.java:
- UUID constants: private final UUID userId = UUID.randomUUID();
- Ticket factory: createTicket(UUID id, User user, TicketStatus status, String ticketNumber)
- Edge case patterns: "shouldThrowWhenNotFound", "shouldThrowWhenNotOwned", "shouldThrowWhenWrongStatus"
- verify with never(): verify(ticketRepository, never()).save(any())

From AgentTicketServiceTest.java:
- ArgumentCaptor: @Captor for event verification
- Multiple user factories: createUser(), createAgent()
- Domain patterns: `ticket.setAssignedTo(agent)` for modifying builder-built entities
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deepen AuthServiceTest + TicketServiceTest + AgentTicketServiceTest</name>
  <files>
    backend/src/test/java/com/shiftleft/hub/user/service/AuthServiceTest.java
    backend/src/test/java/com/shiftleft/hub/ticket/service/TicketServiceTest.java
    backend/src/test/java/com/shiftleft/hub/agent/service/AgentTicketServiceTest.java
  </files>
  <behavior>
    - AuthService: empty/null email registration, malformed password, user logged in as different workspace, refresh with expired token
    - TicketService: null CreateTicketRequest fields, ticket cancellation with empty reason, ticket from wrong workspace user, getTicketsByUser when user disabled
    - AgentTicketService: claimTicket with null/empty agent email, addWorkNote with blank content, getWorkNotes for ticket with no notes (already tested), resolveTicket with empty resolution notes
  </behavior>
  <action>
    Expand the three existing test files with edge-case tests following D-07 (key edge cases: empty/null inputs, not-found, authorization mismatches, boundary conditions).

    **AuthServiceTest.java:**
    Add tests for:
    1. `register_shouldThrowWhenEmailBlank` — empty email throws IllegalArgumentException or service-specific exception
    2. `register_shouldThrowWhenPasswordTooShort` — weak password (<8 chars) validation
    3. `login_shouldThrowWhenEmailNull` — null login parameter
    4. `refresh_shouldThrowWhenTokenExpired` — expired refresh token scenario
    5. `register_shouldSetCorrectDefaultRole` — verify the saved user has ROLE_USER by default

    **TicketServiceTest.java:**
    Add tests for:
    1. `createTicket_shouldThrowWhenIssueBlank` — empty issue string throws validation
    2. `getTicketById_shouldThrowWhenTicketBelongsToDifferentUser` — workspace isolation violation (user tries to access another user's ticket)
    3. `cancelTicket_shouldThrowWhenAlreadyCancelled` — idempotency guard on already-cancelled ticket
    4. `createTicket_shouldGenerateSequentialNumbers` — verify TKT-NNNN number generation increments correctly across two consecutive calls

    **AgentTicketServiceTest.java:**
    Add tests for:
    1. `addWorkNote_shouldThrowWhenContentBlank` — empty/whitespace-only work note content
    2. `listTickets_shouldHandleNullFiltersGracefully` — all null filter params returns all tickets (already tested partially, consolidate assertion)
    3. `claimTicket_shouldRejectAlreadyClaimedTicket` — ticket already claimed by another agent throws IllegalStateException
    4. `resolveTicket_shouldThrowWhenNotAssignedToAnyAgent` — unassigned ticket cannot be resolved

    Follow existing patterns:
    - Section comment headers (// ── methodName ─────────────────────────────────)
    - Factory methods for entity construction
    - `@Mock`/`@InjectMocks` (do NOT add new annotations unless a new dependency is needed)
    - `private final UUID` constants for test identifiers
    - `verify()` after success, `verify(..., never())` for failure cases
    - Each test = exactly one observable behavior assertion
    - No trivial tests per D-08
  </action>
  <verify>
    <automated>cd backend && mvn test -pl . -Dtest="AuthServiceTest,TicketServiceTest,AgentTicketServiceTest" -DfailIfNoTests=false -q 2>&1 | tail -20</automated>
  </verify>
  <done>AuthServiceTest has 14+ tests, TicketServiceTest has 13+ tests, AgentTicketServiceTest has 17+ tests. All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deepen ArticleServiceTest + CategoryServiceTest + TagServiceTest</name>
  <files>
    backend/src/test/java/com/shiftleft/hub/article/service/ArticleServiceTest.java
    backend/src/test/java/com/shiftleft/hub/category/service/CategoryServiceTest.java
    backend/src/test/java/com/shiftleft/hub/tag/service/TagServiceTest.java
  </files>
  <behavior>
    - ArticleService: empty title/content validation, duplicate article detection, workspace isolation, tag not found on create
    - CategoryService: category with empty name, circular parent reference, workspace isolation, not-found on delete
    - TagService: tag with empty name, duplicate tag in workspace, workspace isolation
  </behavior>
  <action>
    Expand three service test files with edge cases.

    **ArticleServiceTest.java:**
    Add tests for:
    1. `createArticle_shouldThrowWhenTitleBlank` — empty title validation
    2. `createArticle_shouldThrowWhenContentBlank` — empty content validation
    3. `getArticle_shouldThrowWhenWrongWorkspace` — article exists but belongs to different workspace
    4. `updateArticle_shouldHandlePartialUpdate` — only updating one field preserves others
    5. `deleteArticle_shouldThrowWhenArticleNotFound` — deletion with non-existent ID

    **CategoryServiceTest.java:**
    Add tests for:
    1. `createCategory_shouldThrowWhenNameBlank` — empty category name
    2. `getCategory_shouldThrowWhenNotFound` — non-existent category ID
    3. `updateCategory_shouldRejectCircularParent` — setting parent to own ID or descendant
    4. `deleteCategory_shouldThrowWhenHasChildCategories` — cascade prevention
    5. `getCategories_shouldReturnEmptyListWhenNoneExist` — empty workspace has no categories

    **TagServiceTest.java:**
    Add tests for:
    1. `createTag_shouldThrowWhenNameBlank` — empty tag name
    2. `createTag_shouldThrowWhenDuplicateInWorkspace` — same tag name already exists in workspace
    3. `getTags_shouldReturnEmptyListWhenNoneExist` — empty workspace returns empty list
    4. `deleteTag_shouldThrowWhenTagNotFound` — non-existent tag deletion
    5. `findByNameEn_shouldReturnEmptyWhenNoMatch` — no tag matches the search term

    Follow existing patterns per D-10 and D-11.
  </action>
  <verify>
    <automated>cd backend && mvn test -pl . -Dtest="ArticleServiceTest,CategoryServiceTest,TagServiceTest" -DfailIfNoTests=false -q 2>&1 | tail -20</automated>
  </verify>
  <done>ArticleServiceTest has 10+ tests, CategoryServiceTest has 10+ tests, TagServiceTest has 8+ tests. All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deepen WorkspaceServiceTest + KcsDraftingServiceTest + remaining test files</name>
  <files>
    backend/src/test/java/com/shiftleft/hub/workspace/service/WorkspaceServiceTest.java
    backend/src/test/java/com/shiftleft/hub/kcs/service/KcsDraftingServiceTest.java
    backend/src/test/java/com/shiftleft/hub/document/service/DocumentServiceTest.java
    backend/src/test/java/com/shiftleft/hub/document/service/DocumentChunkingServiceTest.java
    backend/src/test/java/com/shiftleft/hub/common/config/HrSeederTest.java
    backend/src/test/java/com/shiftleft/hub/common/config/MasterSeederTest.java
  </files>
  <behavior>
    - WorkspaceService: duplicate workspace name, soft-delete idempotency, workspace member count verification
    - KcsDraftingService: dedup on identical events, AI failure handling (mock ChatClient throwing), empty chat transcript
    - DocumentService: duplicate content hash, empty document content, workspace isolation on query
    - DocumentChunkingService: empty text returns empty chunks, single word edge case, max chunk size boundary
    - Seeder tests: idempotent re-run doesn't create duplicates, missing workspace context handled gracefully
  </behavior>
  <action>
    Expand the remaining test files with edge-case coverage.

    **WorkspaceServiceTest.java:**
    Add 2-3 tests:
    1. `createWorkspace_shouldThrowWhenNameBlank` — empty workspace name validation
    2. `createWorkspace_shouldThrowWhenNameAlreadyExists` — duplicate workspace name in tenant
    3. `softDelete_shouldBeIdempotent` — calling delete twice doesn't error

    **KcsDraftingServiceTest.java:**
    Add 3-4 tests:
    1. `onTicketResolved_shouldSkipWhenDuplicateEvent` — identical TicketResolvedEvent produces no second draft
    2. `onTicketResolved_shouldHandleAiFailureGracefully` — mock ChatClient.call() throws, service logs and continues per KCS-04
    3. `onTicketResolved_shouldUseExistingArticleForContext` — when ticket references existing article, include it in AI prompt context
    4. `approveDraft_shouldThrowWhenAlreadyApproved` — double-approve rejected

    **DocumentServiceTest.java:**
    Add 2-3 tests:
    1. `createDocument_shouldThrowWhenContentHashDuplicate` — same content_hash in workspace rejected per dedup constraint
    2. `getDocuments_shouldReturnEmptyWhenWrongWorkspace` — documents from other workspace not visible
    3. `deleteDocument_shouldHandleNotFoundGracefully` — non-existent document deletion

    **DocumentChunkingServiceTest.java:**
    Add 2-3 tests:
    1. `chunk_shouldReturnEmptyForEmptyText` — empty/null input returns empty list
    2. `chunk_shouldHandleSingleWord` — very short content produces valid chunk
    3. `chunk_shouldRespectTokenLimit` — content exceeding max chunk size is split correctly

    **HrSeederTest + MasterSeederTest:**
    Add 1-2 tests each:
    1. `run_shouldBeIdempotent` — running seeder twice doesn't create duplicate users/workspaces
    2. `run_shouldHandleWorkspaceContext` — seeder uses correct workspace context for data creation

    Follow existing patterns per D-10/D-11:
    - Add section headers matching the service's method names
    - Use existing factory methods where available; add new ones if needed for new entity types
    - Use `@Captor` for verifying event publishing (KcsDraftingService publishes TicketResolvedEvent)
    - Mock AI responses per TSD.md — never call real AI APIs
  </action>
  <verify>
    <automated>cd backend && mvn test -pl . -Dtest="WorkspaceServiceTest,KcsDraftingServiceTest,DocumentServiceTest,DocumentChunkingServiceTest,HrSeederTest,MasterSeederTest" -DfailIfNoTests=false -q 2>&1 | tail -20</automated>
  </verify>
  <done>Each deepened file has at least 2-4 new test methods. `mvn test` passes for all modified files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Mock | Mock repositories never persist; tests verify behavior, not data integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T: Tampering | Mock setup | mitigate | Verify mocks return expected values; use `verify()` after success cases |
| T-18-02 | I: Info Disclosure | Test data | accept | Test data uses fake UUIDs and emails; no real credentials in tests |
</threat_model>

<verification>
1. All modified test files compile and pass: `mvn test` on backend
2. Each existing test file has 2-4 new test methods
3. No trivial tests added (no getter/setter, no "component renders" without assertions)
4. All new tests follow existing patterns: @ExtendWith, section headers, factory methods, verify()
</verification>

<success_criteria>
- AuthServiceTest: ≥14 tests (was 9)
- TicketServiceTest: ≥13 tests (was 9)
- AgentTicketServiceTest: ≥17 tests (was 13)
- ArticleServiceTest: ≥10 tests
- CategoryServiceTest: ≥10 tests
- TagServiceTest: ≥8 tests
- WorkspaceServiceTest, KcsDraftingServiceTest, DocumentServiceTest, DocumentChunkingServiceTest, HrSeederTest, MasterSeederTest: each at least 2-4 new tests
- `mvn test` passes — all backend unit + integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-unit-test-tightening/18-01-SUMMARY.md`
</output>
