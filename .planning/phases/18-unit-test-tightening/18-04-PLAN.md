---
phase: 18-unit-test-tightening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/features/kb/public/article-viewer/article-viewer.component.spec.ts
  - frontend/src/app/features/kb/public/article-list/article-list.component.spec.ts
  - frontend/src/app/features/admin/user-list/user-list.component.spec.ts
  - frontend/src/app/features/admin/taxonomy/taxonomy-tree.component.spec.ts
  - frontend/src/app/features/admin/taxonomy/taxonomy-bulk.component.spec.ts
  - frontend/src/app/features/workspace-switcher/workspace-switcher.component.spec.ts
  - frontend/src/app/features/tickets/escalation-form/escalation-form.component.spec.ts
  - frontend/src/app/features/tickets/ticket-detail/ticket-detail.component.spec.ts
autonomous: true
requirements:
  - TST-07
must_haves:
  truths:
    - "KB public article viewer has tests for loading, formatting, and i18n"
    - "KB public article list has tests for search, pagination, tag facets, and error handling"
    - "Admin user list has tests for user management display"
    - "Taxonomy tree has tests for hierarchical tree rendering and CRUD operations"
    - "Workspace switcher has tests for workspace selection and invitation badge"
    - "Escalation form has tests for form submission and validation"
    - "Ticket detail has tests for loading, cancellation, and display"
  artifacts:
    - path: "frontend/src/app/features/kb/public/article-viewer/article-viewer.component.spec.ts"
      provides: "Article viewer component tests"
      exists: true
    - path: "frontend/src/app/features/kb/public/article-list/article-list.component.spec.ts"
      provides: "Article list component tests"
      exists: true
    - path: "frontend/src/app/features/admin/user-list/user-list.component.spec.ts"
      provides: "Admin user list component tests"
      exists: true
  key_links:
    - from: "Components under test"
      to: "Their services"
      via: "Object-literal mocks with vi.fn() — all HTTP mocked"
      pattern: "vi.fn()"
---

<objective>
Add unit tests for the remaining 8 untested frontend smart components: KB public (article-viewer, article-list), admin (user-list, taxonomy-tree, taxonomy-bulk), workspace-switcher, and tickets (escalation-form, ticket-detail).

Purpose: TST-07 requires expanding smart component coverage to ALL untested components per D-04. These 8 components span the remaining untested areas identified in the CONTEXT.md coverage audit.

Output: 8 new spec files with meaningful test coverage for each smart component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AGENTS.md
@.planning/phases/18-unit-test-tightening/18-CONTEXT.md

<interfaces>
<!-- Existing patterns for quick reference -->

Object-literal mock pattern (from ticket-list.component.spec.ts):
```typescript
const mockService = {
  methodName: vi.fn().mockReturnValue(of(mockData)),
};
await TestBed.configureTestingModule({
  imports: [ComponentUnderTest],
  providers: [
    { provide: RealService, useValue: mockService },
    { provide: TranslationService, useValue: { translate: vi.fn(() => 'label') } },
    provideRouter([]),
  ],
}).compileComponents();
```

Error testing with Subject:
```typescript
const errorSubject = new Subject<unknown>();
mockService.methodName.mockReturnValue(errorSubject.asObservable());
component.ngOnInit(); // or component.loadData();
errorSubject.error(new Error('fail'));
expect(component.isError()).toBe(true);
```

Component summaries (grep the .ts files for key signals/methods):

ArticleViewerComponent:
- article = signal<Article | null>(null)
- isLoading, isError signals
- loadArticle(id) — fetches published article by ID
- Uses ArticleService (getArticle or similar)
- Displays markdown content (ngx-markdown)

ArticleListComponent:
- articles = signal<Article[]>([])
- totalPages, currentPage signals
- searchQuery, selectedTags signals
- searchSubject with debounce
- loadArticles(), onSearch(), onTagSelect(), changePage()
- Uses ArticleService (searchPublished or similar)

AdminUserListComponent:
- users = signal<UserResponse[]>([])
- isLoading, isError signals
- loadUsers(), toggleUserEnabled(userId), updateRole(userId, role)
- Uses AdminUserService or UserService directly

TaxonomyTreeComponent:
- categories = signal<Category[]>([]) with hierarchical structure
- expandedNodes = signal<Set<string>>(new Set())
- addCategory(parentId, name), renameCategory(id, name), deleteCategory(id)
- moveCategory(id, newParentId)
- Uses CategoryService

TaxonomyBulkComponent:
- Handles bulk operations on taxonomy (multi-select, batch actions)
- selectedIds = signal<Set<string>>(new Set())
- addBatch(), deleteBatch(), mergeTo(targetId)

WorkspaceSwitcherComponent:
- workspaces = signal<Workspace[]>([])
- currentWorkspace signal
- switchWorkspace(id), createWorkspace(), manageWorkspaces()
- Uses WorkspaceService
- May include invitation-badge sub-display

EscalationFormComponent:
- issue = signal(''), category, urgency signals
- contextData signal (from AI chat transcript)
- isSubmitting, submitError signals
- submitEscalation(), resetForm()
- Uses TicketService.createTicket (or escalation-specific service)

TicketDetailComponent (different from agent-ticket-detail — user-facing):
- ticket = signal<Ticket | null>(null)
- isLoading, isError signals
- loadTicket(id), cancelTicket(reason)
- Uses TicketService.getTicket, cancelTicket
- Shows full ticket details with status, category, urgency
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create KB public component tests (article-viewer + article-list)</name>
  <files>
    frontend/src/app/features/kb/public/article-viewer/article-viewer.component.spec.ts
    frontend/src/app/features/kb/public/article-list/article-list.component.spec.ts
  </files>
  <behavior>
    - ArticleViewer: loads article on init, shows loading/error states, renders markdown content, handles missing article
    - ArticleList: loads articles on init, filters by search query with debounce, filters by selected tags, paginates results, shows empty and error states
  </behavior>
  <action>
    Create two spec files for KB public components.

    **article-viewer.component.spec.ts:**
    Mock: ArticleService (getPublishedArticle or method used), TranslationService
    Test cases:
    1. `should create` — component instantiation
    2. `should load article on init` — route param ID → service called, article signal populated
    3. `should show loading and loaded states` — isLoading transitions true → false
    4. `should handle article load error gracefully` — error response → isError set, article null
    5. `should handle article not found` — service returns null/404 → appropriate state
    6. `should translate labels via TranslationService` — computed labels call translate()
    7. `should display article metadata` — check title, status, dates rendered in template

    **article-list.component.spec.ts:**
    Mock: ArticleService (searchPublished/findAll with pagination), TranslationService, Router (if navigating to article detail)
    Test cases:
    1. `should create` — component instantiation
    2. `should load articles on init` — service called, articles signal populated
    3. `should show loading and loaded states` — isLoading transitions
    4. `should filter by search query with debounce` — onSearch fires searchSubject, load called after debounce
    5. `should filter by selected tags` — onTagSelect adds/removes tag filter
    6. `should paginate results` — changePage updates currentPage, reloads
    7. `should handle empty search results` — empty array → appropriate empty state
    8. `should handle load error gracefully` — error response → isError set
    9. `should translate labels` — computed labels call translate()
    10. `should navigate to article on select` — clicking article calls Router.navigate (if applicable)

    Use object-literal mocks with vi.fn() pattern.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run --reporter=verbose src/app/features/kb/public/article-viewer/article-viewer.component.spec.ts src/app/features/kb/public/article-list/article-list.component.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>7+ tests for ArticleViewer, 10+ tests for ArticleList. All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create admin component tests (user-list + taxonomy-tree + taxonomy-bulk)</name>
  <files>
    frontend/src/app/features/admin/user-list/user-list.component.spec.ts
    frontend/src/app/features/admin/taxonomy/taxonomy-tree.component.spec.ts
    frontend/src/app/features/admin/taxonomy/taxonomy-bulk.component.spec.ts
  </files>
  <behavior>
    - UserList: loads users, role update succeeds/fails, enable/disable toggle, error handling
    - TaxonomyTree: loads categories as tree, expand/collapse nodes, add/rename/delete category, move category
    - TaxonomyBulk: multi-select categories, batch operations, merge
  </behavior>
  <action>
    Create three spec files for admin components.

    **user-list.component.spec.ts:**
    Mock: UserService or AdminUserService (listUsers, updateUserRole, toggleUserEnabled), TranslationService
    Test cases:
    1. `should create` — component instantiation
    2. `should load users on init` — service called, users signal populated
    3. `should toggle user enabled state` — toggleEnabled calls service, user state updates
    4. `should update user role` — role change calls service, user list refreshes
    5. `should handle update error gracefully` — service error → error signal set, user list preserved
    6. `should show loading and loaded states` — isLoading transitions
    7. `should handle empty user list` — empty array → appropriate empty state

    **taxonomy-tree.component.spec.ts:**
    Mock: CategoryService (getCategories, createCategory, updateCategory, deleteCategory, moveCategory), TranslationService
    Test cases:
    1. `should create` — component instantiation
    2. `should load and display hierarchical categories` — parent-child relationships in signals
    3. `should expand and collapse tree nodes` — expandNode/collapseNode toggle
    4. `should add category at root level` — addCategory with null parentId
    5. `should add child category` — addCategory with parentId
    6. `should rename category` — renameCategory calls updateCategory
    7. `should delete category` — deleteCategory with confirmation
    8. `should move category to new parent` — moveCategory calls service
    9. `should handle empty category tree` — empty array → appropriate state
    10. `should handle errors on CRUD operations` — each operation error sets error signal

    **taxonomy-bulk.component.spec.ts:**
    Mock: CategoryService (batch operations), TranslationService
    Test cases:
    1. `should create` — component instantiation
    2. `should select and deselect categories` — toggle selection
    3. `should select all categories` — selectAll
    4. `should perform batch delete` — deleteBatch calls service
    5. `should perform batch merge to target` — mergeTo calls service
    6. `should handle errors on batch operations` — error signal set

    Use object-literal mocks with vi.fn().
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run --reporter=verbose src/app/features/admin/user-list/user-list.component.spec.ts src/app/features/admin/taxonomy/taxonomy-tree.component.spec.ts src/app/features/admin/taxonomy/taxonomy-bulk.component.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>7+ tests for UserList, 10+ for TaxonomyTree, 6+ for TaxonomyBulk. All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create WorkspaceSwitcher + EscalationForm + TicketDetail tests</name>
  <files>
    frontend/src/app/features/workspace-switcher/workspace-switcher.component.spec.ts
    frontend/src/app/features/tickets/escalation-form/escalation-form.component.spec.ts
    frontend/src/app/features/tickets/ticket-detail/ticket-detail.component.spec.ts
  </files>
  <behavior>
    - WorkspaceSwitcher: loads user workspaces, switches active workspace on select, manages invitations, create workspace flow
    - EscalationForm: validates required fields, submits escalation, shows loading state, handles error, resets form
    - TicketDetail: loads ticket on init, displays full detail, cancel ticket flow, loading and error states
  </behavior>
  <action>
    Create three spec files for remaining components.

    **workspace-switcher.component.spec.ts:**
    Mock: WorkspaceService (getWorkspaces, switchWorkspace, createWorkspace), Router, TranslationService
    Test cases:
    1. `should create` — component instantiation
    2. `should load workspaces on init` — service called, workspaces populated
    3. `should highlight current workspace` — active workspace identified
    4. `should switch workspace on selection` — switchWorkspace calls service, navigates
    5. `should create new workspace dialog flow` — open/close create dialog
    6. `should show invitation badge when pending` — pending invitations indicator
    7. `should handle empty workspace list` — no workspaces → appropriate state

    **escalation-form.component.spec.ts:**
    Mock: TicketService (createTicket), TranslationService
    Test cases:
    1. `should create` — component instantiation
    2. `should not submit when issue is empty` — validation prevents empty submission
    3. `should submit escalation with required fields` — createTicket called with proper data
    4. `should submit escalation with AI context` — when shiftLeftContext provided, included in request
    5. `should show submitting state during submit` — isSubmitting true then false after
    6. `should handle submit error gracefully` — error response → error message displayed
    7. `should reset form after successful submit` — form values cleared
    8. `should translate labels via TranslationService`

    **ticket-detail.component.spec.ts:**
    Mock: TicketService (getTicket, cancelTicket), TranslationService, ActivatedRoute
    Test cases:
    1. `should create` — component instantiation
    2. `should load ticket on init` — route param ID → service called, ticket populated
    3. `should show loading and loaded states` — isLoading transitions
    4. `should display ticket details` — ticket fields mapped to signals
    5. `should cancel ticket with reason` — cancelTicket called with reason
    6. `should cancel ticket without reason` — cancelTicket called with empty reason
    7. `should handle cancel error gracefully` — error during cancel sets error signal
    8. `should handle ticket load error gracefully` — error response → isError set
    9. `should handle ticket not found` — 404 from service → appropriate state
    10. `should translate labels` — computed labels call translate()

    Use object-literal mocks with vi.fn() pattern throughout.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run --reporter=verbose src/app/features/workspace-switcher/workspace-switcher.component.spec.ts src/app/features/tickets/escalation-form/escalation-form.component.spec.ts src/app/features/tickets/ticket-detail/ticket-detail.component.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>7+ tests for WorkspaceSwitcher, 8+ for EscalationForm, 10+ for TicketDetail. All pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Component | All service dependencies mocked — tests verify component logic, not integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | T: Tampering | Mock setup | mitigate | Verify mock calls with toHaveBeenCalledWith() |
| T-18-07 | D: DoS | Component error handling | mitigate | All error paths tested — error signals set, component doesn't crash |
</threat_model>

<verification>
1. All 8 spec files compile and pass: `pnpm test -- --run`
2. Each component has meaningful test coverage — not just "should create" smoke tests
3. No real HTTP calls — all services mocked
4. Test patterns match existing spec files (object-literal mocks, Subjects, vi.fn())
5. All error states and edge cases covered (empty data, loading, error)
</verification>

<success_criteria>
- ArticleViewerComponent: 7+ tests covering load, error, i18n
- ArticleListComponent: 10+ tests covering search, tags, pagination, error
- AdminUserListComponent: 7+ tests covering CRUD and error handling
- TaxonomyTreeComponent: 10+ tests covering hierarchy and CRUD
- TaxonomyBulkComponent: 6+ tests covering batch operations
- WorkspaceSwitcherComponent: 7+ tests covering switch, create, invitations
- EscalationFormComponent: 8+ tests covering validation, submit, error
- TicketDetailComponent: 10+ tests covering load, cancel, error
- `pnpm test -- --run` passes — total frontend tests > 170
</success_criteria>

<output>
After completion, create `.planning/phases/18-unit-test-tightening/18-04-SUMMARY.md`
</output>
