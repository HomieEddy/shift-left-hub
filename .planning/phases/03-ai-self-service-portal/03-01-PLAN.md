---
phase: 03-ai-self-service-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/java/com/shiftleft/hub/ai/domain/AiConfig.java
  - backend/src/main/java/com/shiftleft/hub/ai/domain/AiConfigRepository.java
  - backend/src/main/java/com/shiftleft/hub/ai/api/dto/AiConfigRequest.java
  - backend/src/main/java/com/shiftleft/hub/ai/api/dto/AiConfigResponse.java
  - backend/src/main/java/com/shiftleft/hub/ai/api/dto/TestConnectionResult.java
  - backend/src/main/java/com/shiftleft/hub/ai/api/AiConfigController.java
  - backend/src/main/java/com/shiftleft/hub/ai/service/AiConfigService.java
  - backend/src/main/java/com/shiftleft/hub/ai/service/EmbeddingService.java
  - backend/src/main/java/com/shiftleft/hub/ai/domain/package-info.java
  - backend/src/main/java/com/shiftleft/hub/article/service/ArticleService.java
  - backend/src/main/java/com/shiftleft/hub/config/SecurityConfig.java
  - backend/src/main/java/com/shiftleft/hub/common/config/DataSeeder.java
  - backend/src/main/resources/application.properties
autonomous: true
requirements:
  - AI-02
  - AI-06
  - INF-04
user_setup:
  - service: Ollama
    why: "Local LLM provider for API-key-free demo (default)"
    env_vars:
      - name: OLLAMA_ENDPOINT
        source: "Default http://localhost:11434 if running locally; otherwise configure in admin settings"
    dashboard_config:
      - task: "Pull Ollama models after installation"
        location: "Terminal: `ollama pull llama3.2` and `ollama pull nomic-embed-text`"
must_haves:
  truths:
    - "Admin can configure LLM provider (Ollama/OpenAI) via REST API"
    - "Admin can test LLM connection via REST API"
    - "Admin can trigger re-embedding of all published articles"
    - "Embeddings auto-generate when an article transitions to PUBLISHED status"
    - "All /api/ai/** endpoints require authentication"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Spring AI BOM 2.0.0-M8 + ollama/openai/pgvector starters"
      contains: "spring-ai-bom:2.0.0-M8"
    - path: "backend/src/main/java/com/shiftleft/hub/ai/domain/AiConfig.java"
      provides: "Single-row AI config entity with encrypted API key"
      min_lines: 50
    - path: "backend/src/main/java/com/shiftleft/hub/ai/service/EmbeddingService.java"
      provides: "Embedding generation via Ollama, stores in PGVectorStore"
      min_lines: 60
    - path: "backend/src/main/java/com/shiftleft/hub/ai/api/AiConfigController.java"
      provides: "REST API for LLM config CRUD, test connection, re-embed"
      min_lines: 60
    - path: "backend/src/main/java/com/shiftleft/hub/article/domain/Article.java"
      provides: "pgvector embedding column on Article entity"
      contains: "vector"
    - path: "backend/src/main/java/com/shiftleft/hub/config/SecurityConfig.java"
      provides: "/api/ai/** endpoints require authentication"
      contains: "api/ai"
  key_links:
    - from: "AiConfigController"
      to: "AiConfigService"
      via: "constructor injection"
      pattern: "private final AiConfigService"
    - from: "EmbeddingService"
      to: "PGVectorStore (Spring AI)"
      via: "Spring AI auto-configuration"
      pattern: "VectorStore"
    - from: "ArticleService.publishArticle"
      to: "EmbeddingService"
      via: "event/method call on publish"
      pattern: "embeddingService\\.(generate|reEmbed)"
---

<objective>
Create the AI backend infrastructure: Spring AI dependency setup, LLM provider config storage, embedding service, and REST API for admin LLM settings.

Purpose: Provide the foundation for all AI features — configurable LLM provider (Ollama/OpenAI per D-01), vector embedding on article publish (D-05, D-07), and admin-facing config API with test connection (D-02, D-04).

Output:
- Spring AI 2.0.0-M8 dependencies in pom.xml
- AiConfig entity (single-row DB table, encrypted API key per D-03)
- AiConfigService + AiConfigController (CRUD, test, re-embed APIs)
- EmbeddingService (auto-embed on publish + manual re-embed all)
- Article embedding column (pgvector)
- Security + config wiring
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-self-service-portal/03-CONTEXT.md
@.planning/phases/02-knowledge-base/02-01-SUMMARY.md
@.planning/phases/02-knowledge-base/02-02-SUMMARY.md

Existing code patterns (read once, extract interfaces):
@backend/pom.xml
@backend/src/main/java/com/shiftleft/hub/article/domain/Article.java
@backend/src/main/java/com/shiftleft/hub/article/service/ArticleService.java
@backend/src/main/java/com/shiftleft/hub/config/SecurityConfig.java
@backend/src/main/java/com/shiftleft/hub/common/config/DataSeeder.java
@backend/src/main/resources/application.properties

<interfaces>
From existing codebase (extracted for executor):

SecurityConfig.java:
- Uses constructor injection for UserRepository, JwtService
- `.requestMatchers("/api/auth/**").permitAll()`
- `.requestMatchers("/actuator/health").permitAll()`
- `.requestMatchers(HttpMethod.GET, "/api/articles/**").permitAll()`
- `.requestMatchers("/api/admin/**").hasRole("ADMIN")`

ArticleService.java:
- `@Service @RequiredArgsConstructor @Transactional(readOnly = true)`
- `publishArticle(UUID id)` — sets status to PUBLISHED, sets publishedAt
- Pattern for constructor injection of repositories

DataSeeder.java:
- Implements CommandLineRunner, @Order(1), @Profile("!test")
- Uses JdbcTemplate for DDL operations
- Pattern: `jdbcTemplate.execute("ALTER TABLE article ADD COLUMN IF NOT EXISTS ...")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Spring AI BOM + starters to pom.xml</name>
  <files>
    backend/pom.xml
  </files>
  <action>
    Add Spring AI BOM version 2.0.0-M8 to the existing `<dependencyManagement>` section alongside the existing spring-modulith-bom.

    Add these dependencies to `<dependencies>`:
    1. `org.springframework.ai:spring-ai-starter-model-ollama` — Ollama chat + embedding support
    2. `org.springframework.ai:spring-ai-starter-model-openai` — OpenAI support (optional but compile scope)
    3. `org.springframework.ai:spring-ai-starter-vector-store-pgvector` — PGVectorStore for vector search

    Also add `spring-ai-spring-boot-docker-compose` to test scope for future docker compose integration.

    The BOM import goes into the existing `<dependencyManagement>` block:
    ```xml
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>2.0.0-M8</version>
        <type>pom</type>
        <scope>import</scope>
    </dependency>
    ```

    Do NOT add any Starter parent or change the existing spring-boot-starter-parent.
  </action>
  <verify>
    <automated>cd backend && mvn dependency:resolve -q 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <done>pom.xml compiles with Spring AI dependencies resolved. `mvn dependency:tree` shows spring-ai-starter-model-ollama, spring-ai-starter-model-openai, and spring-ai-starter-vector-store-pgvector in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Create AiConfig entity, repository, and package structure</name>
  <files>
    backend/src/main/java/com/shiftleft/hub/ai/domain/package-info.java
    backend/src/main/java/com/shiftleft/hub/ai/domain/AiConfig.java
    backend/src/main/java/com/shiftleft/hub/ai/domain/AiConfigRepository.java
    backend/src/main/java/com/shiftleft/hub/ai/api/dto/AiConfigRequest.java
    backend/src/main/java/com/shiftleft/hub/ai/api/dto/AiConfigResponse.java
    backend/src/main/java/com/shiftleft/hub/ai/api/dto/TestConnectionResult.java
    backend/src/main/resources/application.properties
    backend/src/main/java/com/shiftleft/hub/config/SecurityConfig.java
    backend/src/main/java/com/shiftleft/hub/common/config/DataSeeder.java
  </files>
  <action>
    Create the `com.shiftleft.hub.ai` package-by-module structure:

    **package-info.java**: `@NullMarked` annotation for JSpecify null safety.

    **AiConfig.java** (JPA Entity):
    - Single-row design: use `@Table(name = "ai_config")`, single row with a fixed dummy ID
    - Fields per D-01/D-03:
      - `id` (UUID, generated)
      - `llmProvider` (String, default "OLLAMA") — stores provider enum value
      - `ollamaEndpointUrl` (String, default "http://localhost:11434")
      - `openaiApiKey` (String, nullable — encrypted at rest, see below)
      - `chatModelName` (String, default "llama3.2")
      - `embeddingModelName` (String, default "nomic-embed-text")
      - `similarityThreshold` (double, default 0.65, per D-15)
      - `embeddingDimension` (int, default 768)
    - Standard Lombok: @Entity, @Getter @Setter, @NoArgsConstructor, @AllArgsConstructor, @Builder
    - Encryption: Use a simple AES utility (built-in javax.crypto) with a base64-encoded key from application.properties (`app.ai.encryption-key`). Encrypt/decrypt in the service layer, not at entity level. Store encrypted value as TEXT column.

    **AiConfigRepository.java**: Standard JpaRepository&lt;AiConfig, UUID&gt;. Add `@Query("SELECT c FROM AiConfig c ORDER BY c.id LIMIT 1") Optional&lt;AiConfig&gt; findSingleConfig()` since it's a single-row table. Add `@Modifying @Query("DELETE FROM AiConfig c") void deleteAll()` for reset.

    **AiConfigRequest.java** (Java record): Fields matching AiConfig settable properties:
    - `String llmProvider` (nullable — null means no change)
    - `String ollamaEndpointUrl`
    - `String openaiApiKey` (plaintext in request, encrypted at rest)
    - `String chatModelName`
    - `String embeddingModelName`
    - `Double similarityThreshold`

    **AiConfigResponse.java** (Java record): All fields from AiConfig EXCEPT openaiApiKey (never expose full key). Include `boolean hasOpenaiKey` instead.

    **TestConnectionResult.java** (Java record):
    - `boolean success`
    - `String message`

    **application.properties** additions:
    ```properties
    # AI Config
    app.ai.encryption-key=shiftleft-ai-key-32-chars-base64-padded
    spring.ai.ollama.base-url=http://localhost:11434
    ```

    **SecurityConfig.java** update:
    Add `.requestMatchers("/api/ai/**").authenticated()` before `.anyRequest().authenticated()` — AI endpoints require auth but not admin (both users and admins need chat access per D-12). The admin-only endpoints within /api/ai/ will be role-checked in the controller.

    **DataSeeder.java** update:
    In the `run()` method, after the admin user seeding, seed the default AiConfig if none exists:
    ```java
    if (aiConfigRepository.count() == 0) {
        AiConfig config = AiConfig.builder()
            .llmProvider("OLLAMA")
            .ollamaEndpointUrl("http://host.docker.internal:11434")
            .openaiApiKey(null)
            .chatModelName("llama3.2")
            .embeddingModelName("nomic-embed-text")
            .similarityThreshold(0.65)
            .embeddingDimension(768)
            .build();
        aiConfigRepository.save(config);
        log.info("Created default AI config (Ollama local)");
    }
    ```
    Note the `http://host.docker.internal:11434` for Docker-to-host access.
    Inject `AiConfigRepository` via constructor (add to existing @RequiredArgsConstructor).
  </action>
  <verify>
    <automated>cd backend && mvn compile -q 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <done>AiConfig entity created, seeded via DataSeeder on fresh DB. SecurityConfig protects /api/ai/**. Compilation passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create AiConfigService with encryption, AiConfigController REST API</name>
  <files>
    backend/src/main/java/com/shiftleft/hub/ai/service/AiConfigService.java
    backend/src/main/java/com/shiftleft/hub/ai/api/AiConfigController.java
    backend/src/main/java/com/shiftleft/hub/ai/domain/AiConfigRepository.java
  </files>
  <action>
    **AiConfigService.java**:
    `@Service @RequiredArgsConstructor @Transactional(readOnly = true)`
    - `getConfig()`: fetch single config, return AiConfigResponse (mask API key)
    - `updateConfig(AiConfigRequest req)`: fetch config, apply non-null fields, encrypt openaiApiKey if provided. Return updated AiConfigResponse. `@Transactional`
    - `getConfigEntity()`: internal method that returns the raw AiConfig entity (for other services like AiChatService)
    - `testConnection(AiConfigRequest req)`: Attempt to connect to the configured LLM provider (per D-04). Send a simple prompt "Return only the word hello." and capture response. Return TestConnectionResult. Handle connection exceptions gracefully — return success=false with the error message. Use Spring AI's ChatClient API.
      - If provider is OLLAMA: use OllamaChatModel directly (auto-configured or manually with endpoint from config)
      - If provider is OPENAI: use OpenAiChatModel
    - AES encryption utilities:
      - `encrypt(String plaintext)`: uses javax.crypto.Cipher with AES/GCM/NoPadding, generates random IV, prepends IV to ciphertext, base64 encodes result
      - `decrypt(String ciphertext)`: reverses the encryption
      - Key loaded from `@Value("${app.ai.encryption-key}")` — convert to 256-bit AES key using SHA-256 hash of the string

    **AiConfigController.java**:
    `@RestController @RequestMapping("/api/ai/config") @RequiredArgsConstructor`
    Endpoints (all require auth via SecurityConfig, admin-only enforced at method level):
    - `GET /` → `getConfig()` returns AiConfigResponse
    - `PUT /` → `updateConfig(@RequestBody AiConfigRequest)` updates config, returns updated AiConfigResponse. Role-check: `@PreAuthorize("hasRole('ADMIN')")`
    - `POST /test` → `testConnection(@RequestBody AiConfigRequest)` returns TestConnectionResult. Role-check: `@PreAuthorize("hasRole('ADMIN')")`
    - `POST /embeddings/reindex` → trigger re-embed via EmbeddingService (injected). Role-check: `@PreAuthorize("hasRole('ADMIN')")` Returns `{ "message": "Re-embedding started" }`.

    Both AiConfigService and AiConfigController should be in the same wave as EmbeddingService — no circular dependencies. AiConfigController takes AiConfigService and EmbeddingService as constructor params.

    Add `@EnableMethodSecurity` if not already present — it is already in SecurityConfig.java.
  </action>
  <verify>
    <automated>cd backend && mvn compile -q 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <done>AiConfigService compiles with encryption. AiConfigController exposes GET/PUT /api/ai/config, POST /api/ai/config/test, POST /api/ai/config/embeddings/reindex. Admin-only endpoints enforced.</done>
</task>

<task type="auto">
  <name>Task 4: Create EmbeddingService + add pgvector column to Article + hook into publish flow</name>
  <files>
    backend/src/main/java/com/shiftleft/hub/ai/service/EmbeddingService.java
    backend/src/main/java/com/shiftleft/hub/article/domain/Article.java
    backend/src/main/java/com/shiftleft/hub/article/service/ArticleService.java
    backend/src/main/java/com/shiftleft/hub/common/config/DataSeeder.java
    backend/src/main/resources/application.properties
  </files>
  <action>
    **EmbeddingService.java**:
    `@Service @RequiredArgsConstructor @Transactional`
    Depends on: `AiConfigRepository` (to get embedding model config), Spring AI's `VectorStore` (auto-configured via pgvector starter).
    
    Key fields injected:
    - `VectorStore vectorStore` (Spring AI auto-configured PGVectorStore)
    - `EmbeddingModel embeddingModel` (Spring AI auto-configured OllamaEmbeddingModel)
    - `ArticleRepository articleRepository`
    - `AiConfigService aiConfigService` (to get embedding model name and provider)

    Methods:
    - `generateEmbedding(String text)`: Use `embeddingModel.embed(text)` to get the embedding vector as `float[]`. Use the configured embedding model name from AiConfig.
    - `storeEmbedding(Article article)`: Generate embedding for article content (EN + FR concatenated with separator). Store via VectorStore as a Document with article metadata (id, title, slug).
    - `generateAndStoreEmbedding(Article article)`: Combines generate + store. Called on publish.
    - `reEmbedAll()`: Fetch all PUBLISHED articles, delete existing embeddings for those articles from VectorStore, then generate and store new embeddings for each. `@Transactional`. Log progress for long operations.

    NOTE on Spring AI 2.0 PGVectorStore: The auto-configured `VectorStore` bean from `spring-ai-starter-vector-store-pgvector` connects to the same PostgreSQL database. The executor should check how PGVectorStore works with the existing pgvector extension (already available in Docker). The VectorStore will manage its own internal collection/table for document storage.

    **Article.java** changes:
    NO changes needed to the entity for the embedding column. The Spring AI PGVectorStore manages embeddings in its own internal table structure (not on the Article entity directly). The Article entity remains unchanged. Remove from files_modified if not needed.

    **ArticleService.java** changes:
    Add `EmbeddingService` as a final field (constructor injection via @RequiredArgsConstructor).
    In `publishArticle(UUID id)`, after saving the article with PUBLISHED status, call:
    ```java
    try {
        embeddingService.generateAndStoreEmbedding(article);
    } catch (Exception e) {
        log.warn("Failed to generate embedding for article {}: {}", id, e.getMessage());
        // Non-blocking — embedding failure should not prevent publish
    }
    ```

    **DataSeeder.java** update:
    Add the pgvector extension setup in `setupFullTextSearch()` method (or a separate `setupVectorSearch()` method called from `run()`):
    ```java
    jdbcTemplate.execute("CREATE EXTENSION IF NOT EXISTS vector");
    ```
    Also add the embedding column on the article table — actually, PGVectorStore creates its own schema. No additional column needed on the article table directly.

    **application.properties** additions:
    ```properties
    # pgvector store
    spring.ai.vectorstore.pgvector.distance-type=COSINE
    spring.ai.vectorstore.pgvector.dimensions=768
    spring.ai.vectorstore.pgvector.schema-name=public
    ```
  </action>
  <verify>
    <automated>cd backend && mvn compile -q 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <done>EmbeddingService creates embeddings via Ollama, stores via PGVectorStore. Article publish triggers embedding. Re-embed all available via controller. Compilation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → AiConfig API | Admin configures LLM provider, API keys cross boundary |
| EmbeddingService → Ollama | Article content sent to embedding model (local network) |
| DB → Application (AiConfig) | Encrypted API key read from DB, decrypted in service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | AiConfigController | mitigate | `@PreAuthorize("hasRole('ADMIN')")` on config mutation endpoints (PUT, POST test, POST reindex) |
| T-03-02 | C | AiConfig entity | mitigate | API key encrypted at rest using AES/GCM with key from `app.ai.encryption-key` property |
| T-03-03 | C | AiConfigController GET | mitigate | AiConfigResponse masks API key — never returns full key, only `hasOpenaiKey: boolean` |
| T-03-04 | E | AiConfig endpoints | mitigate | All /api/ai/** endpoints require authentication in SecurityConfig |
| T-03-05 | D | EmbeddingService | accept | Admin-only re-embed trigger, rate-limited by single request processing |
</threat_model>

<verification>
1. `cd backend && mvn compile -q` succeeds
2. Application starts with `docker compose up` — verify via `curl localhost:8080/actuator/health`
3. `curl -X GET http://localhost:8080/api/ai/config` returns 401 (no auth) — confirm security
4. Authenticated admin GET /api/ai/config returns AiConfigResponse with hasOpenaiKey: false
5. Authenticated admin PUT /api/ai/config updates config successfully
</verification>

<success_criteria>
- Spring AI dependencies compile and resolve
- AiConfig CRUD API works with encrypted API key storage
- Admin-only endpoints enforce role check
- Test connection endpoint validates provider connectivity
- Embedding auto-generates on article publish (non-blocking)
- Re-embed all endpoint accessible (admin-only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-self-service-portal/03-01-SUMMARY.md`
</output>
