---
phase: 19-e2e-test-coverage
plan: 05
type: execute
wave: 3
depends_on:
  - 19-02
  - 19-03
files_modified:
  - e2e/tests/admin.spec.ts
  - e2e/tests/document-ingestion.spec.ts
  - frontend/src/app/features/admin/user-list/user-list.component.html
  - frontend/src/app/features/admin/taxonomy/taxonomy-tree.component.html
  - frontend/src/app/features/admin/documents/document-list.component.html
  - frontend/src/app/features/admin/workspaces/workspace-members.component.html
  - frontend/src/app/features/admin/workspaces/workspace-list.component.html
autonomous: true
requirements:
  - TST-14
  - TST-15
user_setup: []

must_haves:
  truths:
    - "Admin happy path spec covers managing users, managing tags, and approving/rejecting KCS drafts"
    - "Document ingestion happy path spec covers uploading a file, verifying it's indexed, and querying about uploaded content"
    - "Missing testids are added to admin user list, taxonomy tree, document list, workspace members, and workspace list templates"
  artifacts:
    - path: "e2e/tests/admin.spec.ts"
      provides: "Admin e2e tests (TST-14)"
      min_lines: 80
    - path: "e2e/tests/document-ingestion.spec.ts"
      provides: "Document ingestion e2e tests (TST-15)"
      min_lines: 80
  key_links:
    - from: "admin.spec.ts"
      to: "admin.page.ts and kcs-drafts.page.ts"
      via: "AdminPage methods for user management, taxonomy, KcsDraftsPage for draft review"
      pattern: "AdminPage|KcsDraftsPage"
    - from: "document-ingestion.spec.ts"
      to: "documents.page.ts"
      via: "DocumentsPage uploadFile, waitForIndexed, verify content via chat query"
      pattern: "DocumentsPage|ChatPage"
---

<objective>
Create Playwright e2e specs for admin (TST-14) and document ingestion (TST-15) happy paths, adding missing testids to admin templates.

Purpose: Per D-01, create `admin.spec.ts` and `document-ingestion.spec.ts`. Add testids to 5 admin templates (user-list, taxonomy-tree, document-list, workspace-members, workspace-list). Per D-13, use the new `AdminPage` and `DocumentsPage` page objects from Plan 01.

Output:
- `e2e/tests/admin.spec.ts` — manage users, manage tags, approve/reject KCS drafts
- `e2e/tests/document-ingestion.spec.ts` — upload file, verify indexed, query about uploaded content
- 5 admin templates updated with testid attributes
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-e2e-test-coverage/19-CONTEXT.md

<interfaces>
Admin templates that need testids (from 19-CONTEXT.md canonical refs):
- user-list.component.html — No testids yet
- taxonomy-tree.component.html — No testids yet
- document-list.component.html — No testids yet
- workspace-members.component.html — No testids yet
- workspace-list.component.html — No testids yet

Existing KcsDraftsPage methods:
```typescript
async goto(): Promise<void>
async getDraftCount(): Promise<number>
async getFirstDraftTitle(): Promise<string | null>
async approveFirstDraft(): Promise<void>
async rejectFirstDraft(): Promise<void>
async getPendingBadgeCount(): Promise<number>
async expectDraftExists(): Promise<void>
```

Existing testids on KCS draft list template: draft-row, draft-title, approve-draft-btn, reject-draft-btn, modal-confirm-btn

The agent dashboard spec (Plan 04) creates tickets with KCS gap flag, which triggers AI to auto-draft a KCS article. So by the time this admin spec runs, drafts should exist from the KCS pipeline. Per D-03, the spec should be self-contained — use API or seed data if no drafts exist.

Backend seeder users from MasterSeeder.java:
- admin@company.com — ROLE_ADMIN
- hr.user@company.com, it.user@company.com — ROLE_USER
- hr.tech@company.com, it.tech@company.com — ROLE_AGENT

Seeded workspaces: "Human Resources", "Legal", "IT", "Public"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testids to admin templates (user-list, taxonomy-tree, workspace-members, workspace-list)</name>
  <files>
    frontend/src/app/features/admin/user-list/user-list.component.html
    frontend/src/app/features/admin/taxonomy/taxonomy-tree.component.html
    frontend/src/app/features/admin/workspaces/workspace-members.component.html
    frontend/src/app/features/admin/workspaces/workspace-list.component.html
  </files>
  <action>
    Add `data-testid` attributes to interactive elements in 4 admin templates per D-07 (interactive elements only — buttons, inputs, links, dropdowns, form controls, table rows).

    **user-list.component.html:**
    1. `data-testid="admin-user-table"` → the table element (line 30)
    2. `data-testid="admin-user-row"` → each `<tr>` in the user table body `@for` loop (line 85)
    3. `data-testid="admin-user-name"` → user display name `<td>` (line 86)
    4. `data-testid="admin-user-email"` → user email `<td>` (line 87)
    5. `data-testid="admin-user-role"` → role `<td>` (line 88)
    6. `data-testid="admin-user-status"` → status `<td>` (line 102)
    7. `data-testid="admin-edit-role-btn"` → each "Edit Role" button (line 117-122)
    8. `data-testid="admin-toggle-status-btn"` → each enable/disable button (line 123-133)
    9. `data-testid="admin-role-dialog"` → the role edit dialog container (line 151-233)
    10. `data-testid="admin-refresh-btn"` → the refresh button (line 7-11)

    **taxonomy-tree.component.html:**
    1. `data-testid="taxonomy-tree"` → the taxonomy tree container div (line 58, the div with class "taxonomy-tree")
    2. `data-testid="taxonomy-node"` → each tree node div (the `@for` loop divs at lines 61-121 and 125-175). Use `[attr.data-testid]="'taxonomy-node-' + node.category.id"` for top-level and child nodes.
    3. `data-testid="taxonomy-node-name"` → the name `<span>` in each node (line 91-93)
    4. `data-testid="taxonomy-edit-btn"` → each "Edit" button (line 106-111)
    5. `data-testid="taxonomy-delete-btn"` → each "Delete" button (line 112-119)
    6. `data-testid="taxonomy-new-btn"` → the "New Category" button (line 16-22)
    7. `data-testid="taxonomy-merge-btn"` → the "Merge" button (line 8-13)

    **workspace-members.component.html:**
    1. `data-testid="workspace-member-table"` → the member table (line 58)
    2. `data-testid="workspace-member-row"` → each member `<tr>` in the `@for` loop (line 76)
    3. `data-testid="workspace-invite-btn"` → the "Invite" button (line 9-13)
    4. `data-testid="workspace-invite-form"` → the invite form container (line 17-55)
    5. `data-testid="workspace-invite-user-select"` → user select dropdown (line 18-27)
    6. `data-testid="workspace-invite-role-select"` → role select dropdown (line 28-38)
    7. `data-testid="workspace-invite-send-btn"` → the send invite button (line 40-46)
    8. `data-testid="workspace-remove-member-btn"` → each remove button (line 98-105)

    **workspace-list.component.html:**
    1. `data-testid="admin-workspace-list"` → the page container (line 1)
    2. `data-testid="admin-workspace-row"` → each workspace row in the list. Check if there's a table loop or card list. From the existing template, it has a create form at the top and workspaces listed below. Add testid to whatever container holds each workspace entry.
    3. `data-testid="admin-create-workspace-btn"` → the "Create" button (line 7-12)
    4. `data-testid="admin-workspace-name-input"` → the name input in create form (line 24-31)
    5. `data-testid="admin-workspace-desc-input"` → the description textarea (line 36-42)
    6. `data-testid="admin-create-workspace-submit"` → the create submit button (line 48-53)

    Do NOT add testids to decorative or styling-only elements. Add attributes to existing elements without changing any existing behavior, classes, or structure.
  </action>
  <verify>
    <automated>cd frontend && npx tsx -e "const fs=require('fs'); const files=['src/app/features/admin/user-list/user-list.component.html','src/app/features/admin/taxonomy/taxonomy-tree.component.html','src/app/features/admin/workspaces/workspace-members.component.html','src/app/features/admin/workspaces/workspace-list.component.html']; files.forEach(f=>{const c=fs.readFileSync(f,'utf8'); if(!c.includes('data-testid='))throw new Error('No testids in '+f)}); console.log('All 4 admin templates have testids')"</automated>
  </verify>
  <done>
    ~20+ testids added across 4 admin templates (user-list, taxonomy-tree, workspace-members, workspace-list). Interactive elements only per D-07.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add testids to document-list template</name>
  <files>
    frontend/src/app/features/admin/documents/document-list.component.html
  </files>
  <action>
    Add `data-testid` attributes to interactive elements in the document list template per D-07:

    1. `data-testid="doc-upload-zone"` → the drag-and-drop zone div (line 33, with `(drop)="onDrop($event)"`)
    2. `data-testid="doc-file-input"` → the hidden file input (line 47-54, `#fileInput`)
    3. `data-testid="doc-category-select"` → the category select dropdown (line 70-81)
    4. `data-testid="doc-table"` → the document table (line 106)
    5. `data-testid="doc-row"` → each document `<tr>` in the `@for` loop (line 133-189). Use `[attr.data-testid]="'doc-row-' + doc.id"`.
    6. `data-testid="doc-filename"` → filename `<td>` in each row (line 136-138)
    7. `data-testid="doc-status"` → status badge in each row (line 142-149)
    8. `data-testid="doc-create-article-btn"` → each "Create Article" button (line 160-165)
    9. `data-testid="doc-delete-btn"` → each delete button (line 180-185)
    10. `data-testid="doc-reprocess-btn"` → each reprocess button (line 173-178)
    11. `data-testid="doc-upload-status"` → the upload queue status container (line 8-29)
    12. `data-testid="doc-delete-modal"` → the delete confirmation modal (line 198-224)

    Add attributes to existing elements without changing any existing behavior, classes, or structure.
  </action>
  <verify>
    <automated>cd frontend && npx tsx -e "const fs=require('fs'); const c=fs.readFileSync('src/app/features/admin/documents/document-list.component.html','utf8'); ['doc-upload-zone','doc-file-input','doc-category-select','doc-table','doc-row','doc-filename','doc-delete-btn'].forEach(id=>{if(!c.includes('data-testid=\"'+id+'\"'))throw new Error('Missing '+id)}); console.log('document-list testids OK')"</automated>
  </verify>
  <done>
    12 testids added to document-list template covering upload zone, file input, table rows, action buttons, and modals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create admin.spec.ts</name>
  <files>
    e2e/tests/admin.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-14 (admin happy path). Use the **admin-chromium** project.

    Import `{ test, expect }` from '@playwright/test', `AdminPage` from `../../pages/admin.page`, and `KcsDraftsPage` from `../../pages/kcs-drafts.page`.

    **Test 1: Manage users** — Change a user's role and toggle status
    - Navigate to admin users page
    - Verify: user table is visible with rows
    - Find a regular user (e.g., email contains "user@") and open their role dialog
    - Change their role (e.g., to AGENT or back to USER)
    - Close the dialog
    - Verify: role badge updated

    **Test 2: Manage tags (taxonomy)** — Create a new taxonomy category
    - Navigate to admin taxonomy page
    - Click "New Category" button
    - Fill in English and French names (e.g., "E2E Test Category" / "Catégorie de test E2E")
    - Submit
    - Verify: new category appears in the taxonomy tree

    **Test 3: Approve/reject KCS drafts**
    - Navigate to KCS drafts page via `kcsDraftsPage.goto()`
    - Verify: at least one draft exists (or handle empty state gracefully)
    - If drafts exist, approve the first draft
    - Verify: after approval, the draft is removed from the queue (draft count decreased)
    - OR if no drafts exist, skip with verification that the empty state is shown

    Use `test.describe('Admin', ...)` and `test.step()` for grouping.

    **Per D-03 (self-contained):** If KCS drafts don't exist naturally (AI pipeline may not have produced one), the test should gracefully pass by checking for either draft rows or the empty state message. This prevents flakiness from the async KCS pipeline.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/admin.spec.ts','utf8'); ['test.describe','AdminPage','KcsDraftsPage','user.*role|role.*user','taxonomy|category','draft'].forEach(c=>{const re=new RegExp(c,'i'); if(!re.test(content))throw new Error('Missing: '+c)}); console.log('admin.spec.ts valid')"</automated>
  </verify>
  <done>
    admin.spec.ts created with 3 tests (manage users, manage tags, approve/reject KCS drafts). Gracefully handles missing KCS drafts.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create document-ingestion.spec.ts</name>
  <files>
    e2e/tests/document-ingestion.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-15 (document ingestion happy path). Use the **admin-chromium** project (document management is admin-only).

    Import `{ test, expect }` from '@playwright/test', `DocumentsPage` from `../../pages/documents.page`, and `ChatPage` from `../../pages/chat.page`.

    **Test 1: Upload a file and verify indexing**
    - Navigate to admin documents page
    - Upload `e2e/fixtures/sample.md` via the file input (resolve the path relative to CWD)
    - Select a category if the category dropdown is visible (optional — can upload without category)
    - Verify: the upload status message appears
    - Wait for the document to appear in the document list with "READY" or "INDEXED" status (use `waitForIndexed` with a 60s timeout)
    - Verify: the document filename appears in the document table

    **Test 2: Query about uploaded content**
    - After the document is indexed, navigate to the chat page
    - Send a question related to the uploaded document content: "What does the sample document contain?"
    - Wait for AI response using `chatPage.waitForResponse(45000)` — longer timeout for RAG pipeline
    - Per D-09, verify: response appeared (assistant-message visible OR fallback visible). No content assertions.

    **Test 3: Cleanup — delete the uploaded document**
    - Navigate back to admin documents
    - Find the uploaded document row
    - Click delete
    - Confirm delete
    - Verify: document is removed from the list (or status shows deleted)

    Use `test.describe('Document Ingestion', ...)` and `test.step()`.

    **Important:** Use `require('path').resolve('e2e/fixtures/sample.md')` or Playwright's `path` module to resolve fixture file paths. Playwright's `setInputFiles()` accepts absolute paths. The test must run from the project root where `playwright.config.ts` is located.

    The document ingestion uses the existing backend API. The upload flow is:
    1. Select file via the hidden input
    2. Backend processes it asynchronously
    3. Poll until status = READY or INDEXED
    4. Verify the content via AI chat query
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/document-ingestion.spec.ts','utf8'); ['test.describe','DocumentsPage','ChatPage','upload','fixtures','indexed|READY'].forEach(c=>{const re=new RegExp(c,'i'); if(!re.test(content))throw new Error('Missing: '+c)}); console.log('document-ingestion.spec.ts valid')"</automated>
  </verify>
  <done>
    document-ingestion.spec.ts created with 3 tests (upload file, query about uploaded content, cleanup delete). Uses fixture files from Plan 01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e test → admin API | Tests interact with admin endpoints for user management, taxonomy, and documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | E (elevation of privilege) | admin.spec.ts | accept | Uses admin-chromium storage state from auth.setup.ts — appropriate admin role |
| T-19-08 | T (tampering) | document-ingestion.spec.ts | accept | Uploads only committed fixture files from e2e/fixtures/. No arbitrary file upload. |
</threat_model>

<verification>
- admin.spec.ts and document-ingestion.spec.ts files exist with valid TypeScript
- 5 admin templates have testid attributes on interactive elements
- Admin spec gracefully handles missing KCS drafts (no flakiness from async pipeline)
- Document spec uses committed fixture files only
- Both specs are fully independent per D-06
</verification>

<success_criteria>
- admin.spec.ts: 3 tests (manage users, manage tags, approve/reject KCS drafts)
- document-ingestion.spec.ts: 3 tests (upload file, query content, delete cleanup)
- ~35 testids added to 5 admin templates
- Both specs fully independent per D-06
</success_criteria>

<output>
After completion, create `.planning/phases/19-e2e-test-coverage/19-05-SUMMARY.md`
</output>
