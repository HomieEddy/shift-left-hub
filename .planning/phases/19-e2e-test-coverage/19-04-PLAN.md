---
phase: 19-e2e-test-coverage
plan: 04
type: execute
wave: 3
depends_on:
  - 19-02
  - 19-03
files_modified:
  - e2e/tests/agent-dashboard.spec.ts
  - e2e/tests/workspace-management.spec.ts
  - frontend/src/app/features/workspace-switcher/workspace-switcher.component.html
autonomous: true
requirements:
  - TST-12
  - TST-13
user_setup: []

must_haves:
  truths:
    - "Agent dashboard happy path spec covers viewing queue, resolving a ticket, and flagging for KCS"
    - "Workspace management happy path spec covers switching workspace and managing members"
    - "Workspace switcher gets testid attributes for toggle, dropdown items, and switch confirm button"
  artifacts:
    - path: "e2e/tests/agent-dashboard.spec.ts"
      provides: "Agent dashboard e2e tests (TST-12)"
      min_lines: 60
    - path: "e2e/tests/workspace-management.spec.ts"
      provides: "Workspace management e2e tests (TST-13)"
      min_lines: 60
  key_links:
    - from: "agent-dashboard.spec.ts"
      to: "agent-dashboard.page.ts"
      via: "Existing AgentDashboardPage methods (claim, resolve, KCS gap)"
      pattern: "AgentDashboardPage"
    - from: "workspace-management.spec.ts"
      to: "workspace-management.page.ts"
      via: "New page object methods for workspace switching and member management"
      pattern: "WorkspaceManagementPage"
---

<objective>
Create Playwright e2e specs for agent dashboard (TST-12) and workspace management (TST-13) happy paths, adding workspace switcher testids.

Purpose: Per D-01, create `agent-dashboard.spec.ts` and `workspace-management.spec.ts`. Add testids to the workspace switcher component. Per D-13, use existing `AgentDashboardPage` and the new `WorkspaceManagementPage` from Plan 01.

Output:
- `e2e/tests/agent-dashboard.spec.ts` — view queue, resolve ticket, flag for KCS
- `e2e/tests/workspace-management.spec.ts` — switch workspace, manage members
- Testids added to workspace-switcher template
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-e2e-test-coverage/19-CONTEXT.md

<interfaces>
Existing AgentDashboardPage methods:
```typescript
async goto(): Promise<void>
async gotoQueue(): Promise<void>
async waitForTickets(timeout?: number): Promise<void>
async getTicketCount(): Promise<number>
async claimTicket(): Promise<void>
async claimFirstTicket(): Promise<void>
async fillResolutionNotes(text: string): Promise<void>
async clickKnowledgeGapCheckbox(): Promise<void>
async clickResolve(): Promise<void>
async getTicketStatus(): Promise<string>
```

Existing testids: agent-claim, agent-resolution-notes, agent-knowledge-gap, agent-resolve, agent-confirm-resolve, ticket-resolved, claim-modal, claim-modal-content, resolve-confirm-modal

Workspace switcher template: toggle button (no testid), dropdown items (no testid), confirm/cancel buttons (no testid).

Seed workspace names: "Human Resources", "Legal", "IT", "Public"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace switcher testids</name>
  <files>
    frontend/src/app/features/workspace-switcher/workspace-switcher.component.html
  </files>
  <action>
    Add `data-testid` attributes to interactive elements in workspace-switcher.component.html per D-07:

    1. `data-testid="workspace-toggle"` → toggle button `(click)="toggleDropdown()"`
    2. `data-testid="workspace-current-name"` → span with `currentWorkspace()?.name`
    3. `data-testid="workspace-dropdown"` → dropdown container div inside `@if (isOpen())`
    4. Use `[attr.data-testid]="'workspace-option-' + ws.id"` on each workspace button in the `@for` loop (`(click)="promptSwitch(ws)"`)
    5. `data-testid="workspace-switch-modal"` → the switch confirmation modal container
    6. `data-testid="workspace-switch-confirm"` → confirm button `(click)="confirmSwitch()"`
    7. `data-testid="workspace-switch-cancel"` → cancel button `(click)="cancelSwitch()"`
  </action>
  <verify>
    <automated>cd frontend && npx tsx -e "const fs=require('fs'); const html=fs.readFileSync('src/app/features/workspace-switcher/workspace-switcher.component.html','utf8'); ['workspace-toggle','workspace-current-name','workspace-dropdown','workspace-switch-modal','workspace-switch-confirm','workspace-switch-cancel'].forEach(id=>{if(!html.includes('data-testid=\"'+id+'\"'))throw new Error('Missing '+id)}); if(!html.includes('workspace-option'))throw new Error('Missing workspace-option'); console.log('OK')"</automated>
  </verify>
  <done>
    7 testids added to workspace-switcher: toggle, current-name, dropdown, option-*, switch-modal, switch-confirm, switch-cancel
  </done>
</task>

<task type="auto">
  <name>Task 2: Create agent-dashboard.spec.ts</name>
  <files>
    e2e/tests/agent-dashboard.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-12 (agent dashboard happy path). Use the **agent-chromium** project.

    Import `{ test, expect }` from '@playwright/test' and `AgentDashboardPage`.

    Per D-03, seed data already provides the user/admin/agent accounts. The spec must create its own test data (a ticket) before agent can resolve it. Use `page.request` API calls to create a ticket via backend API, or rely on seeded test data if a ticket exists in the default seed state.

    Approach: Since this spec runs after escalation.spec.ts creates tickets in the same DB (but per D-06, specs are independent), use API seeding in `beforeEach` to create a ticket:

    ```typescript
    test.beforeEach(async ({ page }) => {
      // Create a ticket via API for the agent to resolve
      await page.request.post('/api/tickets', {
        data: { issue: 'E2E test ticket - agent dashboard', category: 'SOFTWARE', urgency: 'MEDIUM' }
      });
    });
    ```

    **Test 1: View agent queue and claim a ticket**
    - Navigate to agent dashboard via `agentDashboardPage.goto()`
    - Wait for tickets via `agentDashboardPage.waitForTickets()`
    - Verify: ticket count is greater than 0
    - Claim the first ticket via `agentDashboardPage.claimFirstTicket()`
    - Wait for ticket detail page to load

    **Test 2: Resolve a ticket and flag for KCS**
    - After claiming, fill resolution notes
    - Check the knowledge gap checkbox
    - Click resolve and confirm
    - Verify: ticket status shows "Resolved"

    Use `test.step()` for grouping. Use a single `test.describe('Agent Dashboard', ...)` block.

    **Important:** Per D-03, the spec must be self-contained. The `beforeEach` API call creates the ticket the agent will claim. Clean up in `afterEach` if needed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/agent-dashboard.spec.ts','utf8'); ['test.describe','AgentDashboardPage','claim','resolve'].forEach(c=>{if(!content.includes(c))throw new Error('Missing: '+c)}); console.log('agent-dashboard.spec.ts valid')"</automated>
  </verify>
  <done>
    agent-dashboard.spec.ts created with 2 tests (view queue + claim, resolve + KCS flag). Uses API seeding in beforeEach per D-03.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create workspace-management.spec.ts</name>
  <files>
    e2e/tests/workspace-management.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-13 (workspace management happy path). Use the **chromium** project (regular user).

    Import `{ test, expect }` from '@playwright/test' and `WorkspaceManagementPage` from `../../pages/workspace-management.page`.

    **Test 1: Switch workspace**
    - Navigate to a page where the workspace switcher is visible (e.g., `/articles`)
    - Open the workspace switcher dropdown
    - Select a different workspace (e.g., switch from default to "Public" or "Legal")
    - Confirm the switch
    - Verify: the current workspace name in the toggle reflects the new workspace

    **Test 2: View workspace members** (using admin-chromium project context since member management is in admin panel)
    - For this test, use `admin-chromium` storage state. Create a new browser context with `.auth/admin.json`
    - Navigate to admin workspace members page
    - Verify: member list is visible
    - Toggle to an admin-accessible workspace and verify member entries exist

    Since workspace member management is an admin function, this test needs admin privileges. Use `browser.newContext({ storageState: '.auth/admin.json' })` similar to the golden-path pattern.

    Use `test.step()` for grouping. Use `test.describe('Workspace Management', ...)`.

    **Note:** The workspace member management UI is in the admin panel under workspace detail/members tab. The WorkspaceManagementPage from Plan 01 provides helper methods. The test should:
    - Navigate to an admin workspace detail page
    - Click members tab/section
    - Verify member table shows members
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/workspace-management.spec.ts','utf8'); ['test.describe','WorkspaceManagementPage','switch','member'].forEach(c=>{if(!content.includes(c))throw new Error('Missing: '+c)}); console.log('workspace-management.spec.ts valid')"</automated>
  </verify>
  <done>
    workspace-management.spec.ts created with 2 tests (switch workspace, view members). Uses chromium + admin-chromium contexts for role-appropriate access.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e test → agent dashboard API | Tests interact with ticket API endpoints for data seeding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | E (elevation of privilege) | agent-dashboard.spec.ts | accept | Uses existing agent-chromium storage state from auth.setup.ts — same agent role as seeded test data |
</threat_model>

<verification>
- agent-dashboard.spec.ts and workspace-management.spec.ts files exist with valid TypeScript
- Workspace switcher has 7 testids added
- Agent dashboard spec creates its own ticket data via API per D-03
- Workspace management spec uses appropriate browser contexts for role-based access
</verification>

<success_criteria>
- agent-dashboard.spec.ts: 2 tests (queue+claim, resolve with KCS flag)
- workspace-management.spec.ts: 2 tests (switch workspace, view members)
- 7 testids added to workspace-switcher template
- Both specs are fully independent per D-06
</success_criteria>

<output>
After completion, create `.planning/phases/19-e2e-test-coverage/19-04-SUMMARY.md`
</output>
