---
phase: 19-e2e-test-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - e2e/tests/ai-chat.spec.ts
  - e2e/tests/escalation.spec.ts
  - frontend/src/app/features/chat/chat.component.html
autonomous: true
requirements:
  - TST-10
  - TST-11
user_setup: []

must_haves:
  truths:
    - "AI chat happy path spec covers asking a question, receiving streamed response, and rating the answer"
    - "Escalation happy path spec covers requesting human agent, ticket created, and agent views and resolves"
    - "Chat feedback buttons (chat-feedback-yes, chat-feedback-no) testids are added to the template"
    - "AI response assertion uses presence-only check per D-09"
  artifacts:
    - path: "e2e/tests/ai-chat.spec.ts"
      provides: "AI chat e2e tests (TST-10)"
      min_lines: 60
    - path: "e2e/tests/escalation.spec.ts"
      provides: "Escalation e2e tests (TST-11)"
      min_lines: 80
  key_links:
    - from: "ai-chat.spec.ts"
      to: "chat.page.ts"
      via: "ChatPage.sendMessage, waitForResponse, feedback buttons"
      pattern: "ChatPage|chat-feedback"
    - from: "escalation.spec.ts"
      to: "chat.page.ts and tickets.page.ts"
      via: "ChatPage escalation flow, TicketsPage ticket verification"
      pattern: "ChatPage|TicketsPage"
---

<objective>
Create Playwright e2e specs for AI self-service (TST-10) and escalation (TST-11) happy paths, adding chat feedback button testids.

Purpose: Per D-01, create `ai-chat.spec.ts` and `escalation.spec.ts`. Add testids for feedback buttons (`chat-feedback-yes`, `chat-feedback-no`) which are referenced in ChatPage but missing from the template per the canonical refs. AI response assertions use presence-only check per D-09.

Output:
- `e2e/tests/ai-chat.spec.ts` — ask question, receive streamed response, rate answer
- `e2e/tests/escalation.spec.ts` — request human agent, ticket created, agent views and resolves
- `chat-feedback-yes` and `chat-feedback-no` testids added to chat component template
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-e2e-test-coverage/19-CONTEXT.md

<interfaces>
Existing ChatPage methods (from e2e/pages/chat.page.ts):
```typescript
async sendMessage(text: string): Promise<void>
async waitForResponse(timeout = 30000): Promise<boolean>  // Returns true if AI responded, false if fallback
async isFallbackVisible(): Promise<boolean>
async clickEscalate(): Promise<void>
async clickDidNotSolve(): Promise<void>
async submitEscalationForm(category: string, urgency: string): Promise<void>
async getConfirmationTicketNumber(): Promise<string>
```

ChatPage locators already defined:
- `this.feedbackYesButton = this.page.getByTestId('chat-feedback-yes')`
- `this.feedbackNoButton = this.page.getByTestId('chat-feedback-no')`

These exist as locators but the template does NOT have the corresponding testid attributes yet. The chat template has `<button (click)="handleFeedback(true)">` and `<button (click)="handleFeedback(false)">` without testids.

Existing escalation-form template testids: escalation-issue, escalation-category, escalation-urgency, escalation-submit
Existing TicketsPage: createEscalation(), gotoMyTickets(), ticketCount(), getFirstTicketNumber()

Existing AgentDashboardPage methods: gotoQueue(), waitForTickets(), getTicketCount(), claimFirstTicket(), fillResolutionNotes(), clickKnowledgeGapCheckbox(), clickResolve(), getTicketStatus()

Bearer token pattern for API calls within tests: Tests can obtain the token from localStorage or make direct API calls using `page.request()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chat feedback button testids</name>
  <files>
    frontend/src/app/features/chat/chat.component.html
  </files>
  <action>
    Add `data-testid` attributes to the chat feedback buttons per D-07 (interactive elements only). The ChatPage object already defines `this.feedbackYesButton` and `this.feedbackNoButton` locators using `getByTestId('chat-feedback-yes')` and `getByTestId('chat-feedback-no')` — but the template doesn't have these attributes yet.

    In the `@if (showFeedback())` block (around line 109-129), add testids to the two feedback buttons:

    1. `data-testid="chat-feedback-yes"` → the "Yes" button (first button after "Did this solve your problem?" text) — the button with `(click)="handleFeedback(true)"`
    2. `data-testid="chat-feedback-no"` → the "No" button (second button) — the button with `(click)="handleFeedback(false)"`

    Also add `data-testid="chat-feedback-prompt"` to the container div (the div with class `flex items-center gap-3`) to make it easier to wait for the feedback prompt to appear.

    Do NOT add testids to any other elements in the template. The chat component already has testids on: chat-messages (div), assistant-message (div), chat-escalate (button), chat-input (input), chat-send (button).
  </action>
  <verify>
    <automated>cd frontend && npx tsx -e "const fs=require('fs'); const html=fs.readFileSync('src/app/features/chat/chat.component.html','utf8'); const ids=['chat-feedback-yes','chat-feedback-no','chat-feedback-prompt']; ids.forEach(id=>{if(!html.includes('data-testid=\"'+id+'\"'))throw new Error('Missing '+id)}); console.log('All 3 testids found')"</automated>
  </verify>
  <done>
    3 testids added to chat template: chat-feedback-yes, chat-feedback-no, chat-feedback-prompt
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ai-chat.spec.ts</name>
  <files>
    e2e/tests/ai-chat.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-10 (AI self-service happy path). Use the **chromium** project.

    Import `{ test, expect }` from '@playwright/test' and `ChatPage` from `../../pages/chat.page`.

    **Test 1: Ask question and receive streamed response**
    - Navigate to `/chat`, wait for networkidle
    - Send a message using `chatPage.sendMessage('How do I reset my network password?')`
    - Wait for response using `chatPage.waitForResponse(30000)`
    - Per D-09, assert that a response appeared (presence check only). If `waitForResponse` returns true, verify an `assistant-message` element is visible. If false (fallback triggered), verify the `chat-escalate` button is visible (fallback shown).
    - Do NOT assert on AI response content per D-09

    **Test 2: Rate the answer**
    - After receiving a response and the feedback prompt appears:
    - Wait for the `chat-feedback-prompt` container to be visible
    - Click `chat-feedback-yes` to rate the answer positively
    - Verify: feedback prompt is no longer visible (answered flow — the `showFeedback()` signal goes to false)
    - This validates that the feedback/rating interaction works end-to-end

    Use `test.step()` for logical groups. Use `test.describe('AI Self-Service', ...)`.

    **Important per D-09:** Never assert on AI response content text. Only check for presence of assistant-message (success) or escalate button (fallback). The spec must tolerate model variance, fallback triggers, and slow responses.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/ai-chat.spec.ts','utf8'); const checks=['test.describe','ChatPage','sendMessage','waitForResponse','feedback','assistant-message'].every(c=>content.includes(c)); console.assert(checks,'Missing required elements'); console.log('ai-chat.spec.ts valid')"</automated>
  </verify>
  <done>
    ai-chat.spec.ts created with 2 tests (ask+receive, rate answer). AI assertions use presence-only check per D-09.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create escalation.spec.ts</name>
  <files>
    e2e/tests/escalation.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-11 (escalation happy path). This spec spans two roles: user creates a ticket, agent resolves it.

    Import `{ test, expect }` from '@playwright/test', `ChatPage`, `TicketsPage`, and `AgentDashboardPage`.

    **Test 1: User requests human agent and ticket is created**
    - Use the **chromium** project (authenticated user context from storageState)
    - Navigate to `/chat`
    - Send a question: `chatPage.sendMessage('How do I configure my VPN?')`
    - Wait for response via `chatPage.waitForResponse()`
    - If fallback is visible, click `chatPage.clickEscalate()`. Otherwise click `chatPage.clickDidNotSolve()` then wait for escalate button and click `chatPage.clickEscalate()`
    - Fill escalation form using `chatPage.submitEscalationForm('NETWORK', 'HIGH')`
    - Get ticket number from confirmation via `chatPage.getConfirmationTicketNumber()`
    - Verify: ticket number matches pattern `TKT-\d{4}`
    - Navigate to `/tickets` and verify the ticket appears in the list

    **Test 2: Agent views and resolves the ticket**
    - Create a new browser context with agent storage state: `.auth/agent.json`
    - Create a new page from that context
    - Instantiate AgentDashboardPage
    - Navigate to agent dashboard
    - Wait for tickets to load
    - Claim the first ticket
    - Fill resolution notes and flag as knowledge gap
    - Click resolve
    - Verify: ticket status shows "Resolved"
    - Close agent context

    Use `test.step()` for logical groupings. Follow the existing golden-path pattern for multi-context tests (line 82-115 of golden-path.spec.ts).

    **Important:** This test depends on the AI response flow. If the AI responds successfully (not fallback), the escalation path goes through feedback → "No" → escalate. Handle both paths gracefully.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/escalation.spec.ts','utf8'); const checks=['test.describe','ChatPage','TicketsPage','AgentDashboardPage','escalate','resolve','browser.newContext','TKT-'].every(c=>content.includes(c)); console.assert(checks,'Missing required elements'); console.log('escalation.spec.ts valid')"</automated>
  </verify>
  <done>
    escalation.spec.ts created with 2 tests (user creates ticket, agent resolves). Uses cross-context pattern, handles AI fallback/feedback paths gracefully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e test → chat component | Tests interact with AI chat, which may contact external OpenAI API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | I (info disclosure) | ai-chat.spec.ts question text | accept | Question text is generic KB query ("How do I reset my network password?"). No real credentials or PII sent to AI. |
| T-19-05 | E (elevation of privilege) | escalation.spec.ts agent context | accept | Agent storage state is pre-authenticated via auth.setup.ts with seeded agent credentials. No privilege escalation. |
</threat_model>

<verification>
- ai-chat.spec.ts and escalation.spec.ts exist with valid TypeScript
- Chat component has chat-feedback-yes, chat-feedback-no, chat-feedback-prompt testids
- AI response assertions use presence-only check (no content assertions)
- Escalation spec handles both AI success and fallback paths
- Both specs are fully independent per D-06
</verification>

<success_criteria>
- ai-chat.spec.ts: 2 tests (ask+receive, rate answer) with lenient AI assertions per D-09
- escalation.spec.ts: 2 tests (user creates ticket, agent resolves) with cross-context pattern
- 3 testids added to chat template matching existing ChatPage locators
- Both specs fully independent per D-06
</success_criteria>

<output>
After completion, create `.planning/phases/19-e2e-test-coverage/19-03-SUMMARY.md`
</output>
