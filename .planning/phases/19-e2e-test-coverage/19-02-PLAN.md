---
phase: 19-e2e-test-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - e2e/tests/auth.spec.ts
  - e2e/tests/kb.spec.ts
  - frontend/src/app/features/auth/register/register.component.html
  - frontend/src/app/features/kb/public/article-viewer/article-viewer.component.html
  - frontend/src/app/features/kb/public/article-list/article-list.component.html
autonomous: true
requirements:
  - TST-08
  - TST-09
user_setup: []

must_haves:
  truths:
    - "Auth happy path spec exists covering register, login, logout, and protected route redirect"
    - "KB happy path spec exists covering browse articles, search, view bilingual content"
    - "Missing testids are added to register, article viewer, and article list templates"
    - "Both specs use chromium project (user storage state) and follow fully-independent pattern per D-06"
  artifacts:
    - path: "e2e/tests/auth.spec.ts"
      provides: "Auth e2e tests (TST-08)"
      min_lines: 60
    - path: "e2e/tests/kb.spec.ts"
      provides: "Knowledge Base e2e tests (TST-09)"
      min_lines: 60
  key_links:
    - from: "auth.spec.ts"
      to: "register.component.html"
      via: "register-reg-email, register-reg-password, register-submit testid selectors"
      pattern: "data-testid"
    - from: "kb.spec.ts"
      to: "article-viewer.component.html"
      via: "testid on article viewer content loading"
      pattern: "data-testid"
---

<objective>
Create Playwright e2e specs for authentication (TST-08) and knowledge base (TST-09) happy paths, adding necessary testids to templates.

Purpose: Per D-01 (separate spec per feature), create `auth.spec.ts` and `kb.spec.ts` as independent spec files. Add `data-testid` attributes to the register, article viewer, and article list templates per D-07/D-08 (interactive elements only, added during spec writing).

Output:
- `e2e/tests/auth.spec.ts` — register, login, logout, protected route redirect
- `e2e/tests/kb.spec.ts` — browse articles, search, view bilingual content
- Testids added to 3 template files
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-e2e-test-coverage/19-CONTEXT.md

<interfaces>
Existing seed credential pattern from auth.setup.ts:
```typescript
const email = process.env.E2E_USER_EMAIL ?? 'user@shiftleft.com';
const password = process.env.E2E_USER_PASSWORD ?? 'ShiftLeft!2026';
```

Existing LoginPage:
```typescript
async login(email: string, password: string): Promise<void> {
  await this.page.getByTestId('login-email').fill(email);
  await this.page.getByTestId('login-password').fill(password);
  await this.page.getByTestId('login-submit').click();
  await this.page.waitForURL('**/articles');
  await this.page.waitForLoadState('networkidle');
}
```

Existing KnowledgeBasePage methods:
- `goto()` — navigate to /articles, click "Search Articles" link
- `search(query: string)` — fills search, waits for API response, waits for results
- `getResultTitles(): Promise<string[]>` — returns visible result titles
- `openArticle(index: number)` — clicks search result link, waits for article viewer

Existing testids: login-email, login-password, login-submit, nav-logout, kb-search-input, kb-search-result

MasterSeeder users (from MasterSeeder.java):
- admin@company.com / [from config] — ROLE_ADMIN
- it.user@company.com — ROLE_USER (IT workspace)
- hr.user@company.com — ROLE_USER (HR workspace)
- Seeded password comes from app configuration / env vars
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testids to register.component.html</name>
  <files>
    frontend/src/app/features/auth/register/register.component.html
  </files>
  <action>
    Add `data-testid` attributes to interactive elements in the register form template, per D-07 (interactive elements only — buttons, inputs, links, dropdowns, form controls). Add the following testids:

    1. `data-testid="register-name"` → the displayName input
    2. `data-testid="register-email"` → the email input (currently id="reg-email")
    3. `data-testid="register-password"` → the password input (currently id="reg-password")
    4. `data-testid="register-submit"` → the submit button

    Do NOT add testids to decorative elements (SVG icons, password strength bars, list items, etc.) per D-07. Add the attributes on the existing `<input>` and `<button>` elements without changing any existing behavior, classes, or structure.
  </action>
  <verify>
    <automated>cd frontend && npx tsx -e "const fs=require('fs'); const html=fs.readFileSync('src/app/features/auth/register/register.component.html','utf8'); const ids=['register-name','register-email','register-password','register-submit']; ids.forEach(id=>{if(!html.includes('data-testid=\"'+id+'\"'))throw new Error('Missing '+id)}); console.log('All 4 testids found')"</automated>
  </verify>
  <done>
    4 testids added to register form: register-name, register-email, register-password, register-submit
  </done>
</task>

<task type="auto">
  <name>Task 2: Add testids to article-viewer and article-list templates</name>
  <files>
    frontend/src/app/features/kb/public/article-viewer/article-viewer.component.html
    frontend/src/app/features/kb/public/article-list/article-list.component.html
  </files>
  <action>
    Add `data-testid` attributes to interactive elements in the KB templates, per D-07.

    **article-viewer.component.html:**
    1. `data-testid="article-viewer"` → the `<article>` element (line 28)
    2. `data-testid="article-back-link"` → the back link `<a>` (line 2-6)
    3. `data-testid="article-fallback-notice"` → the bilingual fallback notice div (line 32, currently has a class-based selector)

    **article-list.component.html:**
    1. `data-testid="article-list"` → the grid container div (line 30, has class `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`)
    2. `data-testid="article-card"` → each article card div inside the loop (line 33, the div with class `bg-surface-primary rounded-xl shadow-sm...`)
    3. `data-testid="article-card-title"` → the `<h2>` inside each card (line 48-52)
    4. `data-testid="article-list-error"` → the error message paragraph (line 15-18)
    5. `data-testid="article-browse-all"` → the "Browse All Articles" link (line 7, currently a routerLink `<a>` with "Search Articles" text)

    Do NOT add testids to decorative elements (tag chips, author names, dates, pagination controls — pagination is covered via article card selectors). Add attributes on existing elements without changing any existing behavior.
  </action>
  <verify>
    <automated>cd frontend && npx tsx -e "const fs=require('fs'); const viewer=fs.readFileSync('src/app/features/kb/public/article-viewer/article-viewer.component.html','utf8'); const list=fs.readFileSync('src/app/features/kb/public/article-list/article-list.component.html','utf8'); const viewerIds=['article-viewer','article-back-link','article-fallback-notice']; const listIds=['article-list','article-card','article-card-title','article-list-error','article-browse-all']; viewerIds.forEach(id=>{if(!viewer.includes('data-testid=\"'+id+'\"'))throw new Error('Viewer missing '+id)}); listIds.forEach(id=>{if(!list.includes('data-testid=\"'+id+'\"'))throw new Error('List missing '+id)}); console.log('All testids found')"</automated>
  </verify>
  <done>
    3 testids in article-viewer, 5 testids in article-list. All testids on interactive elements per D-07.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create auth.spec.ts</name>
  <files>
    e2e/tests/auth.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-08 (auth happy path). Per D-06, the spec must be fully independent — sets up its own state, no prescribed run order.

    Use the **chromium** project (authenticated as regular user via storage state). Create a test describe block with the following tests:

    **Test 1: Register a new user**
    - Navigate to `/register`, wait for networkidle
    - Fill display name (`register-name`), email (unique timestamp-based), password (valid: meets requirements)
    - Submit (`register-submit`)
    - Wait for redirect to `/articles` or `/login` with success message
    - Verify: URL is not `/register` (successful registration redirects away)

    **Test 2: Login with valid credentials**
    - Use a regular user from seed data (e.g., `it.user@company.com`). The actual password should use the same seeded password from auth.setup.ts environment variable pattern.
    - Navigate to `/login`
    - Fill email and password using existing `login-email` and `login-password` testids
    - Submit using `login-submit`
    - Wait for redirect to `/articles`
    - Verify: `nav-logout` is visible (authenticated)

    **Test 3: Logout**
    - After logging in, click `nav-logout`
    - Wait for login/register nav links to appear
    - Verify: logout button is gone, sign-in/register links are visible

    **Test 4: Protected route redirect**
    - Clear any existing session (use `page.context().clearCookies()` or evaluate to clear localStorage)
    - Navigate to a protected route like `/chat`
    - Verify: redirected to `/login` (or login page is shown with login form visible)

    Import `{ test, expect }` from '@playwright/test'. Use `test.describe('Authentication', ...)` block. Use `test.step()` for logical grouping within tests. Use the existing LoginPage page object for the login flow.

    **Important:** For the register test, use a unique email with timestamp to avoid collisions on re-runs. Use API cleanup in `afterEach` (per D-04) if the test creates a persistent user — call the backend API to delete the created user by email.

    The backend cleanup API endpoint pattern (from existing backend conventions) would be: `DELETE /api/admin/users/{email}` or similar admin endpoint. Use `page.request.delete()` for API calls from within the test.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/auth.spec.ts','utf8'); const checks=['test.describe','import.*test.*expect.*playwright','register','login','logout','protected'].every(c=>content.includes(c)); console.assert(checks,'Missing required elements'); console.log('auth.spec.ts structure valid')"</automated>
  </verify>
  <done>
    auth.spec.ts created with 4 tests (register, login, logout, protected route redirect). All tests follow Playwright best practices (test.describe, test.step, expect assertions). Fully independent per D-06.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create kb.spec.ts</name>
  <files>
    e2e/tests/kb.spec.ts
  </files>
  <action>
    Create an independent Playwright test spec for TST-09 (knowledge base happy path). Per D-06, fully independent.

    Use the **chromium** project. Import `{ test, expect }` from '@playwright/test' and the existing `KnowledgeBasePage` from `../../pages/knowledge-base.page`.

    **Test 1: Browse articles list**
    - Navigate to `/articles`
    - Wait for networkidle
    - Verify: article cards are visible (using `article-card` testid)
    - Verify: at least one article card title exists (using `article-card-title`)
    
    **Test 2: Search articles**
    - Use the existing KnowledgeBasePage
    - Navigate via `kbPage.goto()`
    - Search for a term that exists in seeded articles (e.g., "password" — the seed data includes password policy articles from MasterSeeder's `OLD_ARTICLE_SLUGS` which includes "password-policy-account-security")
    - Wait for search results (existing `waitForResults()` or new `article-card` testid selector)
    - Verify: at least one result is returned
    - Verify: result titles contain the search term or related content

    **Test 3: View article content**
    - Navigate to `/articles` and open the first article by clicking on an article card link
    - Wait for the article viewer to load
    - Verify: the `article-viewer` element is visible
    - Verify: article content is loaded (not empty)

    **Test 4: Bilingual content switching** (per TST-09 requirement)
    - Open an article
    - Switch language using the language toggle (EN/FR buttons in the nav). The EN button is identifiable by text "EN" and the FR button by text "FR"
    - Verify: after switching to FR, the article content area is still rendered (no error state)
    - Switch back to EN

    Use `test.step()` for logical grouping. Use `test.describe('Knowledge Base', ...)` block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('e2e/tests/kb.spec.ts','utf8'); const checks=['test.describe','KnowledgeBasePage','browse','search','article','bilingual'].every(c=>content.includes(c)); console.assert(checks,'Missing required elements'); console.log('kb.spec.ts structure valid')"</automated>
  </verify>
  <done>
    kb.spec.ts created with 4 tests (browse, search, view article, bilingual switch). Uses KnowledgeBasePage page object. Fully independent per D-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e test → Django/Angular app | Tests interact with live frontend via Playwright automation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | I (info disclosure) | auth.spec.ts register flow | accept | Registration uses unique timestamp-based email; API cleanup removes test users. No real credentials leaked. |
| T-19-03 | T (tampering) | register.component.html testids | accept | Testid attributes are static DOM attributes — they do not affect component logic or data flow. |
</threat_model>

<verification>
- auth.spec.ts and kb.spec.ts files exist with valid TypeScript
- All 4 auth tests and 4 KB tests defined with proper assertions
- Register template has 4 testids, article-viewer has 3, article-list has 5
- Specs use projects from existing playwright.config.ts (chromium for user-authenticated context)
</verification>

<success_criteria>
- auth.spec.ts: 4 tests covering register, login, logout, and protected route redirect
- kb.spec.ts: 4 tests covering browse, search, view article, and bilingual content switching
- 12 new testids added to 3 template files
- Both specs are fully independent per D-06 (no cross-test state dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/19-e2e-test-coverage/19-02-SUMMARY.md`
</output>
