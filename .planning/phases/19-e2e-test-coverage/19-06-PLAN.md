---
phase: 19-e2e-test-coverage
plan: 06
type: execute
wave: 4
depends_on:
  - 19-04
  - 19-05
files_modified:
  - e2e/tests/golden-path.spec.ts
autonomous: false
requirements:
  - TST-16
user_setup: []

must_haves:
  truths:
    - "The old golden-path.spec.ts is deleted per D-02, its coverage replaced by the 8 per-feature specs"
    - "All 8 Playwright specs pass consistently (TST-08 through TST-15)"
    - "Backend tests pass: `mvn test`"
    - "Frontend tests pass: `npm run test -- --watch=false`"
  artifacts:
    - path: "e2e/tests/golden-path.spec.ts"
      provides: "Deleted per D-02 — logic extracted into per-feature specs"
      min_lines: 0
  key_links:
    - from: "All 8 new specs"
      to: "TST-16 gate"
      via: "pnpm e2e verifies all pass"
      pattern: "pnpm e2e"
---

<objective>
Delete the old golden-path.spec.ts per D-02 and verify the TST-16 gate: all 8 Playwright specs plus backend and frontend tests pass consistently.

Purpose: Per D-02, extract and discard golden-path.spec.ts. Since all per-feature specs already cover the golden-path scenarios (TST-08 through TST-15), the old file is redundant. Per D-15/TST-16, verify all three test suites pass.

Output:
- Deleted `e2e/tests/golden-path.spec.ts`
- Verified test gate: `pnpm e2e` (8 specs), `mvn test` (backend), `npm run test -- --watch=false` (frontend)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-e2e-test-coverage/19-CONTEXT.md

References to golden-path in CONTEXT.md:
- D-02: "Extract and discard golden-path.spec.ts — lift its reusable logic (page object methods, auth flow) into the new per-feature specs, then delete the old file."
- The golden-path tests covered login → AI query → escalate (now in TST-10/TST-11), agent resolve (now in TST-11/TST-12), and admin KCS drafts (now in TST-14)
- All page objects, test patterns, and flow logic from golden-path are already embedded in the 8 new per-feature specs

Backend test command: `mvn test` (from backend directory)
Frontend test command: `npm run test -- --watch=false` (from frontend directory)
E2E test command: `pnpm e2e` (from frontend directory, uses playwright.config.ts in root)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete golden-path.spec.ts and verify no remaining references</name>
  <files>
    e2e/tests/golden-path.spec.ts
  </files>
  <action>
    Delete `e2e/tests/golden-path.spec.ts` per D-02.

    Before deleting, verify:
    1. All the page objects and interaction patterns used by golden-path are already available in the new per-feature specs:
       - LoginPage → used by auth.spec.ts
       - ChatPage (sendMessage, waitForResponse, escalate) → used by ai-chat.spec.ts and escalation.spec.ts
       - TicketsPage → used by escalation.spec.ts
       - AgentDashboardPage → used by agent-dashboard.spec.ts and escalation.spec.ts
       - KcsDraftsPage → used by admin.spec.ts
    2. The golden-path's browser context patterns (newContext with storageState) are replicated in escalation.spec.ts and workspace-management.spec.ts
    3. No other file imports from golden-path.spec.ts (check via grep)

    After deletion, verify the file is removed from git tracking.

    Steps:
    ```
    git rm e2e/tests/golden-path.spec.ts
    ```
    Verify deletion:
    ```
    if (Test-Path "e2e/tests/golden-path.spec.ts") { throw "File still exists" } else { "File deleted" }
    ```
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'e2e/tests/golden-path.spec.ts') { exit 1 } else { Write-Output 'golden-path.spec.ts deleted' }"</automated>
  </verify>
  <done>
    golden-path.spec.ts deleted. No remaining imports or references to golden-path in the codebase.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all tests pass (TST-16 gate)</name>
  <files></files>
  <action>
    Run the TST-16 gate verification across all three test suites:

    1. **Backend tests**: `cd backend && mvn test`
       - Expect: BUILD SUCCESS, all tests pass
       - If tests fail, note the failures and fix them

    2. **Frontend unit tests**: `cd frontend && npm run test -- --watch=false`
       - Expect: All tests pass (exit code 0)
       - If tests fail, note the failures and fix them

    3. **E2E tests**: `cd frontend && pnpm e2e`
       - Expect: All 8 specs pass (or the subset that can run given the current environment state)
       - Note: E2E tests require `docker compose up` running with seeded data
       - Playwright config has retries=0 locally (fail fast for debugging), workers=1 (serial per D-10)

    **Strategy for failures:**
    - If backend or frontend tests fail, investigate and fix (likely testid additions introduced lint/test issues)
    - If e2e tests fail, most common issues:
      - `data-testid` attribute doesn't match the locator in the spec or page object
      - Wait timeouts — increase timeouts or adjust wait strategy
      - AI response timeouts — the AI model may be slow or unavailable
    - Fix issues and re-run until all three suites pass

    **Prerequisites for E2E tests:**
    - Docker stack must be running (`docker compose up`) with seeded data
    - Frontend dev server must be running (`pnpm start`)
    - Playwright browsers installed (`npx playwright install chromium`)
    - Auth storage state files exist (created by `setup` project automatically)
  </action>
  <verify>
    <automated>cd frontend && pnpm e2e</automated>
  </verify>
  <what-built>
    All 8 Playwright e2e test specs plus the backend and frontend test suites. The golden-path.spec.ts has been deleted and its coverage absorbed into the per-feature specs.
  </what-built>
  <how-to-verify>
    1. Ensure Docker stack is running: `cd backend && docker compose up -d`
    2. Ensure backend is running: `curl http://localhost:8080/api/health`
    3. Ensure frontend dev server is running: `cd frontend && pnpm start` (port 4200)
    4. Run backend tests: `cd backend && mvn test`
    5. Run frontend tests: `cd frontend && npm run test -- --watch=false`
    6. Run e2e tests: `cd frontend && pnpm e2e`
    7. Verify all three suites pass with zero failures
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any failures found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new code introduced in this plan | Only file deletion and test execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | N/A | golden-path.spec.ts deletion | accept | No new code — file deletion only. No security impact. |
</threat_model>

<verification>
- golden-path.spec.ts is deleted and not referenced by any other file
- `cd backend && mvn test` passes
- `cd frontend && npm run test -- --watch=false` passes
- `cd frontend && pnpm e2e` passes (all 8 specs)
</verification>

<success_criteria>
- golden-path.spec.ts deleted per D-02
- TST-16 gate: all three test suites pass
- Phase 19 success criteria met (ROADMAP.md items 1-4)
</success_criteria>

<output>
After completion, create `.planning/phases/19-e2e-test-coverage/19-06-SUMMARY.md`
</output>
