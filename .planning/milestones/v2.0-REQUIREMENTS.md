# Requirements Archive: v2.0 Workspace Platform

**Archived:** 2026-06-14
**Status:** ✅ SHIPPED

This file archives all v2.0 requirements as they stood at milestone completion. All requirements were implemented via Phases 9-16.

## v2 Requirements

### Workspace Foundation

- [x] **WSF-01**: User can create a workspace with a name and URL-safe slug — **Implemented** (Phase 9)
- [x] **WSF-02**: Existing domain tables gain a `workspace_id` FK for data isolation — **Implemented** (Phase 9)
- [x] **WSF-03**: JWT carries a `workspace_id` claim — **Implemented** (Phase 9)
- [x] **WSF-04**: Hibernate `@Filter` + AOP enforces workspace isolation — **Implemented** (Phase 9)
- [x] **WSF-05**: pgvector metadata filters all similarity searches — **Implemented** (Phase 9)
- [x] **WSF-06**: v1.0 data migrated to a "Default Workspace" — **Implemented** (Phase 9)

### Document Ingestion

- [x] **DOC-01**: Upload markdown, text, PDF files via drag-and-drop — **Implemented** (Phase 10)
- [x] **DOC-02**: Spring AI ETL pipeline extracts, chunks, embeds — **Implemented** (Phase 10)
- [x] **DOC-03**: Upload status tracking visible to user — **Implemented** (Phase 10)
- [x] **DOC-04**: Documents deduplicated by SHA-256 hash — **Implemented** (Phase 10)
- [x] **DOC-05**: User can reprocess a single document — **Implemented** (Phase 10)

### BYO LLM Configuration

- [x] **LLM-01**: Configure OpenAI-compatible endpoint URL — **Implemented** (Phase 10)
- [x] **LLM-02**: API key encrypted at rest — **Implemented** (Phase 10)
- [x] **LLM-03**: Test LLM connection before saving — **Implemented** (Phase 10)
- [x] **LLM-04**: Model name selection — **Implemented** (Phase 10)
- [x] **LLM-05**: Workspace ChatModelRegistry routes per-workspace — **Implemented** (Phase 10)

### Domain-Agnostic AI

- [x] **DOM-01**: Define domain taxonomy (category → subcategory) — **Implemented** (Phase 11)
- [x] **DOM-02**: Documents/articles support domain metadata — **Implemented** (Phase 11)
- [x] **DOM-03**: Customize system prompt with template variables — **Implemented** (Phase 11)
- [x] **DOM-04**: Unified hybrid search across articles and chunks — **Implemented** (Phase 11)
- [x] **DOM-05**: AI queries scoped to workspace's knowledge base — **Implemented** (Phase 11)

### Workspace Management UI

- [x] **WSM-01**: Workspace switcher dropdown in navigation — **Implemented** (Phase 12)
- [x] **WSM-02**: In-app member invitation with role selection — **Implemented** (Phase 12)
- [x] **WSM-03**: Workspace-scoped roles enforced in UI — **Implemented** (Phase 12)
- [x] **WSM-04**: Admin panel for members, LLM config, documents — **Implemented** (Phase 12)
- [x] **WSM-05**: Leave workspace, admin can delete (soft-delete) — **Implemented** (Phase 12)

### Frontend Cleanup

- [x] **FEC-01**: All components use `templateUrl:` — zero inline templates — **Implemented** (Phase 13)
- [x] **FEC-02**: Multi-component files split into individual files — **Implemented** (Phase 13)
- [x] **FEC-03**: `ng build` compiles without errors — **Implemented** (Phase 13)
- [x] **FEC-04**: Existing frontend tests pass — **Implemented** (Phase 13)

### File Upload Format Support

- [x] **FUF-01**: Upload `.html` files via drag-and-drop — **Implemented** (Phase 15)
- [x] **FUF-02**: Upload `.xml` files via drag-and-drop — **Implemented** (Phase 15)
- [x] **FUF-03**: HTML content extracted, chunked, embedded — **Implemented** (Phase 15)
- [x] **FUF-04**: XML content extracted, chunked, embedded — **Implemented** (Phase 15)
- [x] **FUF-05**: File type validation with clear error messages — **Implemented** (Phase 15)

### UI Neutralization

- [x] **UIN-01**: Neutral landing page messaging — **Implemented** (Phase 16)
- [x] **UIN-02**: Neutral navigation labels — **Implemented** (Phase 16)
- [x] **UIN-03**: Domain-agnostic logos/favicon — **Implemented** (Phase 16)
- [x] **UIN-04**: Neutral empty states and error pages — **Implemented** (Phase 16)
- [x] **UIN-05**: All workspace seeders render with neutral UIs — **Implemented** (Phase 16)

### Seeding Revamp

- [x] **SEED-01**: 4 workspaces seeded: HR, Legal, IT, Public — **Implemented** (Phase 14)
- [x] **SEED-02**: Admin on all workspaces; dept users on dept + Public — **Implemented** (Phase 14)
- [x] **SEED-03**: 10 published articles per workspace with bilingual content — **Implemented** (Phase 14)
- [x] **SEED-04**: 40 seed markdown files under classpath:data/seed/kb/ — **Implemented** (Phase 14)
- [x] **SEED-05**: Old v1.0 migration logic fully removed — **Implemented** (Phase 14)
- [x] **SEED-06**: Seeding fully idempotent — **Implemented** (Phase 14)

## Requirements Moved to v3 (Future)

- **LLM-06**: Embedding model configurable separately from chat model
- **LLM-07**: Per-user model override for power users
- **DOM-06**: Automatic domain detection on document upload
- **DOC-06**: Document preview before indexing
- **DOC-07**: DOCX file support via Tika reader
- **DOC-08**: Batch upload of multiple files (10+)
- **WSM-06**: Workspace templates with pre-seeded categories/prompts/articles
- **WSM-07**: Cross-workspace search across user's workspaces
- **WSM-08**: Usage token tracking dashboard per workspace

## Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| WSF-01–06 | Phase 9 | Complete |
| DOC-01–05 | Phase 10 | Complete |
| LLM-01–05 | Phase 10 | Complete |
| DOM-01–05 | Phase 11 | Complete |
| WSM-01–05 | Phase 12 | Complete |
| FEC-01–04 | Phase 13 | Complete |
| SEED-01–06 | Phase 14 | Complete |
| FUF-01–05 | Phase 15 | Complete |
| UIN-01–05 | Phase 16 | Complete |

**Coverage:** 46/46 requirements implemented across 8 phases.

## Technical Debt Noted at Close

See `.planning/v2.0-MILESTONE-AUDIT.md` for full details.
